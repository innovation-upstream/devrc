# dl-router — copy to ~/.config/dl-router/config.toml and edit.
#
# NOTHING host-specific is committed: this file ships neutral placeholders only.
# The real config, the alias database, the route log, the bearer token and the
# qBittorrent credentials all live outside the repo.

# Absolute path to the media library root. REQUIRED — the sidecar's routing
# endpoints return 503 until it is set. This must ALSO be the Brave profile's
# download.default_directory (see setup-brave-profile.sh): Chrome will not let
# onDeterminingFilename escape the download root.
library_root = "/path/to/library"

# Loopback sidecar. 8790 is already taken on this host, hence 8791.
host = "127.0.0.1"
port = 8791

# Auto-file at or above this score; below it the picker opens. Two candidates
# within `tie_margin` of each other also go to the picker.
#
# The score is NOT the only gate. Only a directory classified `performer` in
# ~/.config/dl-router/dirs.toml may auto-file at all: a `category` directory
# always confirms, and an UNCLASSIFIED one does too. Generate a draft to
# review with:
#
#     dl-route dirs classify --out ~/.config/dl-router/dirs.toml
#
# Until that file exists nothing auto-files, which is the safe end of the
# range, not a misconfiguration.
auto_threshold = 0.75
tie_margin = 0.05

# Catch-all directory: where an unconfident download lands, and where Esc in
# the picker leaves it.
other_dir = "other"

# How long the extension waits for the sidecar before falling back to its
# cached snapshot. Chrome discards a slow suggestion, so keep this short.
match_timeout_ms = 400
# How far back a page-context capture may be reused for a download on the same
# tab (correlation tier 3).
capture_window_s = 15
toast_ms = 8000

# --- duplicate handling -------------------------------------------------------
# A duplicate is NEVER acted on automatically: the file is kept and filed
# normally, and the toast offers `delete` and `keep`.
#
# `delete_mode` is what `delete` does:
#   "trash"  (default) rename into a hidden `.dl-router-trash/` inside the
#            library root. An atomic same-filesystem move, skipped by both
#            index scans, inspectable with `ls`, undone with `mv`.
#   "unlink" really remove the file.
#
# The default is the reversible one deliberately. Note what it does and does
# NOT buy: it protects your BYTES, and nothing else. qBittorrent seeds by
# PATH, so moving a payload into the trash breaks a torrent exactly as
# deleting it would — the seeding guard is the payload check below, not this.
#
# Nothing sweeps the trash. It grows until you empty it by hand:
#   du -sh <library root>/.dl-router-trash
[dedupe]
delete_mode = "trash"

# How long `delete` may spend PROVING the two files are identical before it
# gives up. It reads both files in full and compares them byte for byte —
# sampling a bounded window cannot prove two multi-GB files identical, and a
# check that authorises destruction has to be a proof. Running out of the
# budget is a REFUSAL, never an all-clear. The extension's own /discard
# timeout must exceed this.
verify_timeout_s = 180.0

# --- qBittorrent -------------------------------------------------------------
# Used by `dl-route backfill` AND by `POST /discard`.
#
# Leave `username` empty and the backfill refuses to move anything: without the
# torrent list it cannot prove a file is not a live seeding payload. /discard
# falls back to local structural checks (hardlink count, symlink, sparseness);
# set the credentials and it additionally demands the same live corroboration
# the backfill does, and refuses if qBittorrent is configured but unreachable.
[qbt]
url = "http://127.0.0.1:30880"
username = ""
password = ""
timeout_s = 8.0
# Extra host-side mount roots to consider when deriving the host<->container
# path mapping. The ancestors of library_root are always tried; this is an
# escape hatch for unusual layouts. The mapping is derived AT RUNTIME from
# torrents/info[].save_path — never from qBittorrent's stored config, whose
# LastSavePath points at a mount that no longer exists.
host_roots = []

# --- yt-dlp ------------------------------------------------------------------
# The HLS/DASH path: a stream has no single file for the download listener to
# save, so the context menu hands the page URL to yt-dlp instead.
[ytdlp]
bin = "yt-dlp"
# Reuse the browser's logged-in session. Format: "brave:<profile directory>".
# Empty disables cookie reuse.
cookies_from_browser = ""
output_template = "%(title).150B [%(id)s].%(ext)s"
max_jobs = 4

# --- per-site capture rules ---------------------------------------------------
# Adding a site is CONFIG, not code. Keys are hostnames; values are CSS
# selectors whose text (or content/title attribute) becomes a subject tag.
# Supported selector subset: tag, .class, #id, [attr], [attr="v"],
# [attr^="v"], descendant combinators, and comma-separated groups.
#
# Generic extraction (Open Graph, JSON-LD Person/VideoObject, [itemprop=name],
# meta keywords) always runs, so most sites need no rule at all.
#
# [site_rules."example-site.test"]
# subject = ["a.performer-name", ".subject-link"]
# tags    = [".tag-list a"]
#
# --- two layers, keyed on two different hosts ---------------------------------
# A forum page's videos are usually CROSS-ORIGIN IFRAMES to an embed host, and
# the two questions have to be asked in two different documents:
#
#   * the CONTEXT layer, keyed on the PAGE host -- what is this page about?
#     That is `subject`/`tags` above (or the explicit `.context` sub-table
#     below, which wins where both exist). Read in the top frame.
#   * the PLAYER layer, keyed on the EMBED host -- where is the media element
#     and how is its URL read? Read INSIDE the embed frame, which is a
#     different origin and therefore a different key in this same table.
#
# Keying the player rule on the forum does not work: the forum's embed wrapper
# is host-agnostic, so it would match embeds from hosts you have no rule for.
# An embed host with no player rule simply gets no button.
#
# [site_rules."forum.example.test".context]
# subject = ["h1.thread-title"]
#
# [site_rules."embedhost.example.test".player]
# container = "#video-container"     # one button per match (required)
# mount     = "#video-wrapper"       # where inside it to mount (optional)
# label     = "Save to library"      # optional, <= 40 chars
# # The media-URL accessor. READ AT CLICK TIME, never cached: these URLs are
# # commonly signed with a short expiry and re-signed in place on the same
# # element, so a value read when the button was drawn is already stale.
# # `attr` is an attribute (or same-named property) NAME, never an expression:
# # config is data, and there is no path here that evaluates a string.
# media     = { element = "video#main-video", attr = "src" }
#
# # `media` may also be an ORDERED LIST of accessors, tried in order, first
# # http(s) hit wins. That exists because `attr` is ONE name, so a single pair
# # cannot cover a site that posts both an image and a video: the image's own
# # `src` is often a downscaled copy from a resizing proxy, with the original on
# # a sibling <a> INSIDE the container, while a video's `src` is usually the
# # original already. Put the accessor that resolves the BEST copy first.
# #
# # This works because each `element` is resolved with
# # container.querySelectorAll -- a DESCENDANT query -- so the anchor does not
# # have to wrap the image. On the chat site this was built for it never does:
# # measured, 0 ancestor <a> of 3 attachments. What was NOT measured is whether
# # that anchor falls inside any particular `container`; confirm yours encloses
# # it.
# #
# # [site_rules."chat.example.test".player]
# # container = "[class^=mediaWrapper]"
# # media = [
# #   { element = 'a[href^="https://cdn.example-cdn.test/attachments"]', attr = "href" },
# #   { element = "video", attr = "src" },
# # ]
# #
# # This accessor is the ONLY route to the original for the player button:
# # `playerDownload` downloads the URL the rule resolves and never rewrites it.
# # (The separate "Save to library..." CONTEXT MENU cannot see a sibling anchor
# # at all -- a browser fills `linkUrl` only from an ANCESTOR link -- so that
# # path reaches the original by rewriting the proxy host instead. The two
# # hosts were measured to carry the SAME signature values, so the rewrite
# # keeps a valid signature. An earlier version of this comment asserted the
# # opposite and told you never to rewrite; it was wrong.)
# #
# # One malformed accessor rejects the WHOLE rule and renders no button --
# # keeping the good ones would silently cover only some media on the page,
# # which looks like the feature working. At most 8 accessors per rule.

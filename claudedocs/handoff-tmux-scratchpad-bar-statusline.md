# Handoff: tmux-scratchpad-bar-statusline — 2026-09-10

## Run this first — the index, one command
```bash
cairn recall --repo /home/zach/workspace/devrc
```

## Goal
Two UX improvements to the scratchpad and tmux statusline:
1. Make the named, colored scratchpad popup hotkeys larger (90%×90% instead of 80%×80%)
2. Remove time/host from tmux status-right, and move the scratchpad status legend (currently in tmux status-left) to the i3status-rust bar — with the full 20-slot list since there's more space there

## State now
🔴 **THIS ARC IS CLOSED.** Three PRs merged, feature deployed and verified live on BOTH hosts, doc-rot closed, and the last unexercised surface — the bar legend's click — was **exercised 2026-09-11** (below). Nothing is in flight, and no next step from this arc is outstanding.

- **Merged** (all verified by CONTENT on `origin/main`, never by ancestry — a squash merge never makes the branch head an ancestor):
  - **#1485** `fe4460d6` — the feature: 90%×90% popups, legend moved to the i3 bar.
  - **#1484** `799d966c` — de-duplicated a Gotchas block this doc's own correction run appended twice.
  - **#1526** `3ca6e098` — doc rot: `scripts/README.md` indexed `tmux-claude-counters.sh`, deleted 4 weeks earlier in `059b7fbe` (#475); `tmux-scratch-slots.sh` carried stale positive-control counts.
- **Deployed:** `LAPTOP_SSH=zach@10.42.0.100 scripts/ship.sh` → converged + verified, both hosts at `3a0c77dd`, 586/532 managed artifacts resolving, **0 dangling, 0 stale** on each. Bars restarted (workbench 934021→1132952, laptop 2568→3736632), both tmux servers reloaded. Workspace and focus recorded before each restart and **unchanged after** on both.
- **Verified live on both bars by screenshot**: 20 slots, per-slot colour, live slots `key+count` bold, absent slots dim `#504945` with no count, positioned between the disk and net pills. Re-verified against live state at close-out: 23 popups at 90%, **0 at 80%**; `status-right` has no `%H:%M`/`#H`; `status-left` has no `scratch-status`; legend emits 20 spans.
- Claim `tmux-scratchpad-bar-statusline-1` RELEASED. No open PRs from this arc; both feature branches deleted.
- 🔴 **The bar legend's left-click is now EXERCISED, not just structurally pinned** (workbench, 2026-09-11, bar PID 1132952 — the arc's own restarted instance, never restarted since). Fired the **verbatim `cmd` string read out of the deployed `config-top.toml`** (not retyped), through `sh -c`, under `env -i` + i3status-rs's own `/proc/<pid>/environ`, via `setsid`. That environment is the point: it genuinely carries **no `$TMUX`** and `TMUX_TMPDIR=/run/user/1000`, and it was asserted absent before the spawn rather than assumed — a naive spawn from inside a tmux client inherits `$TMUX` and tests the *`display-popup`* caller instead.
  - **The picker OPENED and HELD**: `alacritty` (tty `?`) → `fzf` alive on its own pty `pts/100`, state `Sl+`, waiting on input. **A live process on a real pty is the behavioural proof the structural test could not give** — the `read -p` concern was that stdin on `/dev/null` makes bash skip the hold; here fzf owned a pty and blocked on it.
  - **Rendered** (screenshot): `scratch>` prompt, `18/18`, the `enter: toggle | type to filter/create` header, 17 sessions + `[+ new scratchpad]`.
  - 🔴 **The detach defect has a real NEGATIVE CONTROL, not just an absence.** Its precondition was live at the time: a client on `pts/0` attached to an **unrelated, non-`scratch*`** session, on a server whose MRU is a `scratch*` session. Both clients (`pts/0` and `pts/58`) were **still attached afterwards**, on the same sessions. Reaching fzf at all is the second discriminator — the detach branch `exit 0`s before fzf, so a rendered picker proves the branch was not taken.
  - 🔴 **Legend and picker agree BY NAME, closing the "which server" question that was pinned only against a fake tmux.** Legend: 20 spans, 17 live / 3 dim. Live keys `g G v V P o O N w W m i I u U y Y` → scratch, scratch2-4, scratch6-8, scratch10-13, scratch15-20; dim keys `p n M` → scratch5, scratch9, scratch14. The picker listed **exactly** those 17 and **none** of those 3. Counts matching is the weak version of this check — the set identity is the claim.
  - Screen: the float takes FOCUS but no workspace switch (it is floating on the current workspace). Active window and workspace recorded before, both restored after and verified equal. Killed by **resolved PID**, after matching `/proc/<pid>/cmdline`, never a `-f` pattern.
- No clawgate task: `clawgate_handoff.sh resolve` → **rc 5 (NOTHING RESOLVED)**. Its positive control proved the board reachable, but an unknown session id also returns an empty array, so that is **not** a clean bill of health. No field written.

### Carried forward — real, and NOT closed by this arc
- 🔴 **The audit ladder never returned a clean round.** Round 1 (full) → 1 🔴 + 4 🟡 + 4 🟢; round 2 (delta) → 5 🟡 + 4 🟢; all fixed. **Round 3 was dispatched and stopped by the operator**, so `nix/programs/tmux/slot-table.nix` and its three replacement guards — the largest structural change in the PR — **shipped unaudited**. Not a defect claim; simply unexamined. Delta range `ffe4e5d0..7c449cdd`; the round-2 `audit-claims` block is posted on PR #1485.
- ⚠ `drift-check.service` exits **17** (a `nix/pkgs/**` source subtree behind on some host). Pre-existing, predates this arc.
- ⚠ The workbench tree may still carry **another session's** uncommitted `nix/graphical.nix` emoji-fontconfig WIP. Not this effort's; left untouched (no stash, no revert).

## Open investigations — live diagnosis state
_(none — this is a planned feature, not a bug investigation)_

## Next steps (ranked)
_Nothing from this arc is outstanding. Both items below are **carried**, not owed — neither was created by this work._

1. **Resolve `drift-check.service` rc 17.** Pre-existing, unrelated to this arc.
   forcing: none
2. **If the unaudited round-3 machinery misbehaves**, audit `ffe4e5d0..7c449cdd`. Recorded so it is cheap to resume; not scheduled.
   forcing: none

~~**Click the bar legend once.**~~ **DONE 2026-09-11** — exercised on the workbench with a real negative control for the detach defect and name-level legend/picker agreement; see "State now". The click is no longer an unverified surface.

## Gotchas / decisions / dead-ends
- The scratch-status.sh script (`scripts/tmux-scratch-status.sh`) is NOT deleted — it's just no longer called from the tmux statusline. It may still be useful for debugging or as a fallback.
- The bar block is unconditional (both hosts) because scratchpads exist on both hosts, unlike the poller-backed count blocks which are workbench-only.
- No signal needed — the block reads local tmux state directly, no poller/cache. Same pattern as `i3status-claude-runs`.
- Left-click on the bar legend opens `scratch-picker.sh` in a float terminal.
- The continuum-save interpolation (`set -ag status-right`) in `nix/programs/tmux/default.nix` must remain AFTER the simplified status-right — it appends and survives reloads, so the ordering is still correct.

- 🔴 **The plan's load-bearing assumption was unstated and is now MEASURED: per-slot colour inside one bar block works via pango markup.** Nothing in `nix/graphical.nix` or any `scripts/i3status-*` used pango before this, so it was worth proving before building on it. `i3status-rs --version` = **0.36.1**; the man page gives the `custom` block's default format as `"{ $icon|} $text.pango-str() "`. Positive control: a throwaway TOML + command script emitting `{"text":"<span foreground=\"#b8bb26\">g2</span> <span foreground=\"#504945\">G</span>","state":"Idle"}`, run under `timeout 6 i3status-rs <cfg>`, produced the i3bar line
  `{"full_text":" <span foreground=\"#b8bb26\">g2</span> <span foreground=\"#504945\">G</span> ","color":"#93A1A1FF",…,"markup":"pango"}`
  — `markup: "pango"` set, spans passed through **unescaped**. **Corollary: the text IS markup, so any literal `<`/`&`/`>` must be `html.escape`d.**
- **The plan said one `home.file` entry; it needs TWO.** The block script resolves the slot table from its own directory, so `scripts/tmux-scratch-slots.sh` must ALSO be symlinked to `~/.config/i3status-rust/scripts/scratch-slots.sh` — the same sibling-module pattern `claude_sessions.py` uses for `i3status-claude-runs` (`nix/graphical.nix` ~:698). Without it the script's leg-1 lookup misses on a live host.
- **The plan named `.tmux.conf:89` only; `.tmux.conf:104` is the same class of popup.** Line 104 is the `Alt+[` "toggle most recent scratch-\*" popup, also `80% 80%`. Lazygit at line 112 is already `90% 90%`, so 90% is already the house idiom. Deliberately widened to include 104. 🔴 **`.tmux.conf:66` (`fzf-tmux -p 80%,50%`) is NOT in scope** — that is the window switcher, a different feature.
- **The bar click needs a float terminal, not a bare command.** `scratch-picker.sh` is an fzf TUI, and i3status-rust runs clicks through `sh -c` with **no controlling terminal** (the live `i3status-rs` has TTY `?`) — a bare TUI there exits `inappropriate ioctl for device` and the click is a **silent no-op**. Model the click on `syshealthCmd` (`nix/graphical.nix` ~:88): `alacritty --class float,float -o window.dimensions.columns=… -e …`, and interpolate `${home}`, never a literal `~`.
- 🔴 **Deploying this is RESTART-class, not reload-class** (from the cairn `nix` entry, 2026-09-08): i3status-rs reads its TOML **once at startup**, so `ship.sh` + `i3-msg reload` leaves the new block absent on a host reporting a fully successful deploy. A bar/WM restart takes the operator's screen, so it was deliberately left undone — hand it over, do not run it from an agent.
- 🔴 **`pgrep -x i3status-rs` returns EMPTY while the bar is running** — home-manager wraps the binary so the kernel `comm` is `.i3status-rs-wr`, and `-x` matches comm exactly. That zero is a fact about the instrument. Use `pgrep -a i3status-rs`; confirm a restart by the **PID changing** plus `ps -o etimes=`, never by an `-x` count.
- **`scripts/tmux-scratch-status.sh` is NOT deleted** — it stops being called from `status-left` but is kept as a fallback/debug renderer. Its header comment and the `Consumers` list in `scripts/tmux-scratch-slots.sh` both described it as "the status-left legend", which the change makes false; both were in scope to correct. A comment is a claim.
- The continuum autosave interpolation (`set -ag status-right`, appended by `nix/programs/tmux/default.nix`) must stay **after** the plain `set -g status-right`. Editing status-right is safe; introducing a *new* plain `set -g status-right` after it is not. `scripts/tests/test_tmux_continuum_save_interpolation.py` pins this.
- ⚠ On this host `grep` is a function wrapping ugrep and honours `.gitignore` — use `command grep -r` when sweeping for references, or a zero is a claim about grep's view rather than the tree.

- 🔴 **CI caught a real defect that THREE audit rounds did not, and it was in the audits' own scaffolding.** `test_runtime_shebangs.py::test_no_test_writes_a_usr_bin_env_shebang_at_runtime` went red at `7c449cdd`: `test_scratchpads_block.py` wrote its own shebang inline. The fix round had already reasoned its way to the right *interpreter* (absolute bash, because the test's PATH jail holds only shims and `env` cannot resolve bash) but wrote it at the call site, which the guard forbids. Fixed by routing through `testlib.mockbin.write_exec`, which owns the shebang and rejects a body carrying one. **The audits were reading for defects; CI was reading for repo invariants — different questions.** Verify a fix like this with BOTH controls: the guard red-before/green-after, AND that the shims still RUN (the jail test asserts `seen == _PICKER_BINARIES`, which can only hold if every shim started and logged its own name — a shim that fails to start leaves that set empty).
- 🔴 **A `home-manager switch` by ANOTHER session killed a background monitor mid-run, and the failure mode was a busy-loop, not an error.** `~/.nix-profile` is blanked while a switch writes its two profile generations, so a bare-name `sleep` died `No such file or directory`; with no `set -e` the poll loop span on `gh`. MEASURED 2026-09-11: generations 2155→2156 at 11:50:17→11:50:48, a **~31 s window**, not the ~1 s the note in MEMORY describes. **Reproduced the same day at a second point** — generations 2181→2182 at 20:30:35→20:31:10, a **~35 s window**, hit accidentally by an unrelated command while ANOTHER session switched: `/home/zach/.nix-profile/bin/tmux` returned `no such file or directory` having resolved fine two minutes earlier, because the INTERMEDIATE generation does not carry it. Two points ~9 h apart, both ~31-35 s, so the ~1 s figure is wrong by an order of magnitude rather than by chance. **Any long-running script must call binaries by absolute path** and must refuse to continue if its sleep fails rather than spin. Correlate with `~/.local/state/nix/profiles/profile-*-link` mtimes to prove it. 🔴 **But `/run/current-system/sw/bin` is NOT a universal fallback, and this bullet used to say it was.** That works only for binaries the SYSTEM profile carries; a `home.packages` binary is in neither system path. Measured at the second point: `/run/current-system/sw/bin/tmux` and `/etc/profiles/per-user/zach/bin/tmux` are **both absent**, so a script "hardened" that way would have died in exactly the same window. For a user-profile binary the durable handle is the **running process's own `/proc/<pid>/exe`**, or an explicit `/nix/store/…` path — resolve it once, before the window, and reuse it.
- 🔴 **A `home-manager switch` does NOT update a RUNNING tmux server — the statusline is server state.** After the switch, `tmux show -g status-right` still returned the OLD value with `%H:%M` and `#H`. The deployed `~/.config/tmux/tmux.conf` was correct the whole time. `tmux source-file ~/.config/tmux/tmux.conf` applies it; **never kill the server** — that destroys every session. This is the "restart the consumer" step with tmux as the consumer, and it is easy to miss because every file-level check passes.
- ⚠ **`tmux list-keys` QUOTES the popup size flags** (`-h "90%" -w "90%"`), and re-orders them relative to the source. A grep for `-w 90%` unquoted matched **1 of 23** real bindings and would have read as "the change did not land". Count both spellings, and run a positive control proving the pattern can match the value you are claiming is absent.
- **`bash-guard.py` evaluates the branch statically, BEFORE the command runs.** A single call doing `git checkout -b X && … && git commit` is refused, because the guard sees `git commit` while the checkout is still on `main`. Split the checkout and the commit into separate calls. It also cannot resolve a shell variable in `git -C $W` and falls back to judging the caller's cwd — **pass `-C` a literal absolute path**.
- **`${PIPESTATUS[0]}` is empty in zsh** — it is `$pipestatus` (lowercase array). A `cmd | tail; echo "EXIT=${PIPESTATUS[0]}"` printed nothing and a `status=behind` refusal read as success until the repo was checked directly. Capture with `out=$(cmd 2>&1); rc=$?` instead.
- **`handoff_doc.py`'s Gotchas section is an APPEND bucket.** A follow-up run that re-sends the whole delta to correct one line in a REPLACE section duplicates every appended bullet (that is what PR #1484 cleaned up). **A correction run must omit the APPEND sections** — a section the delta omits is left untouched.

- 🔴 **`ship.sh` rc 19 on a first pass is NORMAL when a PR merges mid-run, and the remedy is literally "run it again".** MEASURED 2026-09-11: pass 1 landed the workbench on `bc67b177` and the laptop on `3a0c77dd` — `origin/main` moved between the two legs' fetches. **Every per-host check passed on both**; each host really was at origin/main *as it saw it*. The fleet was in two states and only the cross-host comparison could see it. Pass 2 converged both. This is exactly why the final line is a claim about the two hosts AGREEING on one sha, and why reading the per-host greens alone is not enough.
- 🔴 **`ship.sh` re-execs itself when the run ships a change to `ship.sh`** — it says so (`re-executing the NEW copy (generation 1 of 1)`) and warns that everything printed above it was computed by the OLD copy, because the CONVERGE payload is expanded before the fast-forward. Not an error; do not re-run on account of it.
- **A dirty tracked file that nix READS at eval time changes what a switch builds**, and `ship.sh` classifies this explicitly (`DIRTY AND IN THE ARTIFACT`, 1 of 169 nix-read paths derived from 30 nix files). The honest reading of a live probe on that host is "this is evidence about the DEPLOYED artifact, not about `main`". **The cheap discriminator is the OTHER host**: the laptop's tree was clean and its bar rendered identically, which is what actually rules the dirty hunk out — not reasoning about the diff.
- **A `home-manager switch` does not restart the consumer, and there are TWO consumers here.** The bar needs a restart (i3status-rs reads its TOML once at startup) *and* a running tmux server needs `tmux source-file ~/.config/tmux/tmux.conf` — the statusline is server state, so every file-level check passes while `tmux show -g status-right` still returns the old value. Never `kill-server`; that destroys every session.

- 🔴 **CI caught a real defect that THREE audit rounds did not, and it was in the audits' own scaffolding.** `test_runtime_shebangs.py::test_no_test_writes_a_usr_bin_env_shebang_at_runtime` went red at `7c449cdd`: the new test file wrote its own shebang inline. The fix round had already reasoned its way to the right INTERPRETER (absolute bash, because the test's PATH jail holds only shims and `env` cannot resolve bash) but wrote it at the call site, which the guard forbids. Fixed via `testlib.mockbin.write_exec`, which owns the shebang and rejects a body carrying one. **The audits were reading for defects; CI was reading for repo invariants — different questions, and the ladder cannot substitute for the gate.**
- 🔴 **Verify a shebang-guard fix with BOTH controls, or a green is meaningless.** The guard going red-before/green-after only proves the scanner stopped matching — it does not prove the shims still RUN. The discriminator here was already in the suite: the jail test asserts `seen == _PICKER_BINARIES`, which can only hold if every shim started and logged its own name; a shim that fails to start leaves that set empty. Negative control: reintroducing `#!/usr/bin/env bash` at the same site turned the guard red again naming that line, and the file was restored byte-identical (`cmp`).
- 🔴 **Fix a stale COUNT in prose by fixing the FORM, not the number.** `tmux-scratch-slots.sh` hardcoded "79 live `bindsym` lines" and "22 `M-<key>` entries" as the positive controls behind its load-bearing claim that no i3 binding exists for any slot. Live values were **84** and **26**; nothing asserts on them, so they drifted silently. Renumbering regenerates the defect on the next drift — the controls are now the COMMANDS that re-derive them. (The zero needs a non-empty corpus at all because a zero against an empty corpus is indistinguishable from a grep wired to nothing.)
- **When a doc index is stale, measure the WHOLE index, not the row you were told about.** `scripts/README.md` listed one deleted script; checking all **72** rows showed exactly 1 dangling, so the index was otherwise sound — and that is a different report from "found a bug". 🔴 `scripts/README.md` has **0** entries in `doc-path-baseline.tsv`, i.e. `test_doc_path_rot.py` does not scan it at all, which is why nothing caught this. **No test pins README rows to files that exist** — gap named, not closed.
- **Not every reference to a deleted file is rot.** Of 9 live references to `tmux-claude-counters.sh`, only the README row was wrong: `config-stack.md` describes it in the PAST tense as REMOVED (and is baselined), `drift-check.sh`/`drift_phase2.py` name it as a phase-2 reader awaiting deletion, and two `claudedocs/` files are dated historical records. **Read each hit before "fixing" it.**
- ⚠ **`git grep` vs the `grep` function.** A `find | grep` sweep for the deleted script returned hits that were all inside stale `.claude/worktrees/` copies — a real-looking result about files nobody reads. `git -C <repo> grep -n <pat>` sees tracked files at HEAD only and answered the actual question.

- 🔴 **A CORRECTION TO THIS SESSION'S OWN REPORT: `#1534` was already MERGED when this session called it open.** The close-out run measured the kill-mention red against a control at `622fc2d8`, reported "PRs #1534 and #1522 are open against it", and `#1534` had merged at **01:55:28Z** — six minutes BEFORE this arc's own `#1545` (02:01:49Z) and after the control ran. **A control is evidence about the tree it ran on, and on a repo this busy that tree is stale within minutes.** Re-measure before quoting a CI state, never cite the PR list you read earlier in the session.

- 🔴 **THE KILL-MENTION LEDGER IS A SELF-REGENERATING RED, AND THAT IS THE FINDING — not any one offender.** `test_every_kill_server_call_site_in_the_repo_is_classified` pins the set of tracked files mentioning a wide tmux kill against `_KILL_MENTION_LEDGER` in `scripts/claude-hooks/tests/test_guard_core.py`. Every **prose handoff doc** that quotes such a token reds `main` until someone adds a row. The loop closes on itself: a session finds `main` red → writes a handoff doc *describing* the red → that doc quotes the token → `main` is red again on the new doc. **MEASURED over ~2 h on 2026-09-11/12, three rounds, each fix minting the next offender:**
  1. `handoff-mention-system-repos.md` + `handoff-tmux-webapp.md` → ledgered by **#1534** (01:55Z).
  2. `handoff-cairn-oss-multi-instance.md` → ledgered by **#1557** (`6395ce1f`), whose title is literally *"main is RED again, one hour later, from a doc quoting THIS SCANNER'S OWN OUTPUT"*.
  3. `handoff-gate-speed-and-ci-signal.md` → **still open.** Added by `58c03a77`, a handoff commit titled *"…main found RED"*. Its offending line is the sentence describing the scanner bug: ``scanner bug** (it matched `kill-session` inside `sk`+`ill-session`)``.
  Verified at `origin/main` `6ff4d215`, with `#1557` confirmed an ANCESTOR (`merge-base --is-ancestor` → YES) and the offender carrying **0** ledger rows there — so this is a fresh instance, not #1557 un-landed. The other half, `test_no_tracked_shell_text_writes_a_kill_this_guard_would_deny`, **now passes**; #1534 fixed it.
  ⚠ **Do NOT fix this by adding a fourth ledger row.** A live claim already holds the structural fix — `devrc-kill-ledger-scope-executables` (ZacxDev), *"scope `_KILL_MENTION_LEDGER` to executable paths; claudedocs prose is covered by the shell-text guard"* — with branches `fix/kill-ledger-scope` and `fix/unblock-main-kill-mention-ledger` pushed. A row closes one instance; scoping the ledger ends the loop. `#1522` is open on the same red but touches different files and does **not** cover this one.

- **This red is inherited, not caused, by any docs PR from this arc — and the control is what says so.** `#1545`'s scoped run reported 2 failed; a detached worktree at clean `origin/main` failed the **same two tests with byte-identical assertion detail**. This doc is correctly ledgered and stayed out of the `added:` set throughout, with `removed: []` proving the edits never broke its own two-way pin. 🔴 **Read the `added:` list, not the pass/fail** — the test names the offending files, so attribution costs one line of output and nothing else distinguishes "my change" from "main was already red".

- 🔴 **`ONE MEASUREMENT OF A CI STATE IS A CLAIM ABOUT A MOMENT.` This session measured the same two tests at FOUR trees** — `622fc2d8` (2 failed), `origin/main` twice at `6ff4d215` (1 failed, 1 passed, different offender), and the branch at its new base — and the answer moved every time. The offender rotated, one test flipped green, and a fix merged mid-session. **State the tree sha beside any CI verdict**, or the verdict reads as a property of the repo rather than of one commit.

- ⚠ **The `resume-state.sh` PR-reference scan counts hex colours.** Its one reported gap — *"gh answered for 5 of 6 referenced PR(s)"* — resolved to `#504945`, the dim-slot colour this doc names in two places, not to a PR. A `#[0-9]+` scan over a doc carrying hex literals will always under-report. **Check what the sixth reference actually is before treating a gap as unreconciled state.**

- 🔴 **A CLEAN `git status` at the start of a turn does not survive the turn — another session took `main` out from under this one, MID-RUN.** Reflog: `20:32:03 checkout: moving from main to docs/close-tmux-scratchpad-bar-statusline`, then `20:37:56 checkout: moving from docs/close-tmux-scratchpad-bar-statusline to main` — **not this session**, while a ~5-minute scoped test run was in flight, followed by two `merge origin/main: Fast-forward` (the signature of a concurrent `ship.sh`). The edits survived only because they were uncommitted; a `commit` there would have landed on `main` silently, with `git log` afterwards showing exactly what you expect. **`git branch --show-current` IMMEDIATELY before the commit is what caught it** — the check at the top of the turn had been true and was worthless by then. The fix was not to re-checkout and race: it was `cp` the edit aside (never `stash` — repo-global), restore the shared clone, and redo the work in a dedicated worktree.

- ⚠ **The base clone drifts again within minutes, and that is ordinary churn, not a fault.** Re-synced to `ed0842ca` right after `#1545` merged; `origin/main` was at `6ff4d215` shortly after, with the clone at `6395ce1f`. On a repo landing PRs this fast, `--ff-only` currency is a property of the instant you checked. **Re-sync when you are about to act in the clone, not once per session** — and when another session is live in it, prefer leaving `HEAD` alone over moving it under them.

## How to verify
Re-verified at close-out, on the workbench, all passing:
1. `tmux list-keys -T root | grep -cE '\-[wh] "?90%"?'` → 23; the same with `80%` → **0**. 🔴 tmux QUOTES and re-orders these flags (`-h "90%" -w "90%"`), so an unquoted `-w 90%` pattern matches 1 of 23 and reads as "the change did not land". Run a positive control proving the pattern CAN match the value you claim is absent.
2. `tmux show -g status-right` / `status-left` → no `%H:%M`, no `#H`, no `scratch-status`; `tmux show -g status-right | grep -c continuum_save` → exactly **1**.
3. `grep -c i3status-scratchpads ~/.config/i3status-rust/config-top.toml` → 1; `~/.config/i3status-rust/scripts/i3status-scratchpads | grep -o '<span' | wc -l` → **20**.
4. Bar restart: `pgrep -a i3status-rs` before/after, confirm the **PID changed**. 🔴 Never `pgrep -x` — home-manager wraps the binary so `comm` is `.i3status-rs-wr` and `-x` always returns empty.
5. Screenshot and LOOK: `DISPLAY=:0 XAUTHORITY=~/.Xauthority nix-shell -p maim --run "maim -u out.png"`. The only check that proves pango actually renders.
6. All three slot-table readers agree after any edit to `scripts/tmux-scratch-slots.sh`: bash `source` → 20, `i3status-scratchpads` → 20 spans, and `nix-instantiate --eval` on `nix/programs/tmux/slot-table.nix { path = …; }` → 20.
7. **The legend click** (done 2026-09-11; re-run it this way, not by hand-typing the command). Read the `cmd` OUT of the deployed `~/.config/i3status-rust/config-top.toml` and fire it under the bar's own environment — `env -i "$(tr '\0' '\n' < /proc/<bar-pid>/environ)"`, through `sh -c`, under `setsid`. 🔴 **Assert `TMUX` is absent from that environ before spawning**: a spawn from inside a tmux client inherits `$TMUX` and silently tests the `display-popup` caller instead, which takes a different branch. Pass = an `fzf` process alive on a real pty (`ps -o tty=,stat=` → `pts/N  Sl+`), **not** merely a window appearing; that is what distinguishes "held" from "flashed and vanished". Cross-check the legend's live-slot set against the picker's session list **by name**, not by count. Record active window + workspace first, kill by **resolved PID** after matching `/proc/<pid>/cmdline`, and restore both.

# Task-spec drafter — shadow queue 20260623T133043

Source: ClickUp triage view `6-901111220963-1` · processed 8 of 76 · mode=shadow

## Classification counts

          5 TASK
          3 NEEDS-DECISION

## Action-worthy (TASK / NEEDS-DECISION / VERIFY / DUPLICATE / safety-flagged)

### [NEEDS-DECISION] 868k49796 — Civitai Link key generation hangs/fails on civitai.red (TLS cert + CivitaiLinkProvider error)
- confidence: medium  age: 0d  status: to do
- verified: Confirmed the JS crash in current HEAD (2026-06-23): src/components/CivitaiLink/civitai-link-api.ts:20-23 returns {} on a failed request while getLinkInstances casts to CivitaiLinkInstance[], so array consumers throw the reported 'a.find is not a function'. The Link endpoint is a single build-time NEXT_PUBLIC_CIVITAI_LINK URL (env/client-schema.ts; worker line 39) with no .red/.com handling, consistent with the reported ERR_CERT_COMMON_NAME_INVALID on .red. No commit addresses .red Link key-gen. Could NOT reach the cluster or GitHub PR API (both require approval in this run), so the live Link-service/cert state and whether cross-domain .red Link serving is sanctioned remain unverified.
- correlations: aggregates (same root cause, Link key-gen hang on .red): #67341, #67535, #67659, #67980, #68081 — all cited in this ticket's body as the same symptom; #67341's console error matches the verified code defect; adjacent-to #65536 (request for Civitai Link support on .red / membership value) — related ask, distinct from the bug
- recommendation: Escalate to Zach: the user-facing fix (key generation works on .red) hinges on a TLS cert / cross-domain endpoint change outside this repo plus a product call on whether Civitai Link is meant to serve civitai.red post-split — neither verifiable from available tools. Separately, an in-repo defensive fix is clearly correct regardless of that decision: civitai-link-api.ts clFetch should return [] (or surface a typed error) instead of {} on non-ok responses so a blocked request yields a clean failure state rather than an infinite hang + a.find crash. Confirm cert/endpoint ownership before drafting any infra work.
- ⚠ SAFETY: Cannot verify whether Civitai Link is intended to serve civitai.red after the .com/.red split, nor the live Link-service/cert posture (cluster and PR API unreachable). Drafting a confident 'extend the Link TLS cert/endpoint to .red' task would be harmful if the split deliberately scopes Link to .com — the cert mismatch could be an intentional non-extension rather than an oversight. The in-repo clFetch defensive fix is safe; the cross-domain serving decision is not.

### [TASK] 868k47a7x — LoRA trainer 'Train Further' (and some trainings) use SDXL instead of the selected base model (Illustrious/Anima)
- confidence: medium  age: 0d  status: to do
- verified: Confirmed real and current: the 'Train Further'/continue-training flow shipped Jun 12 2026 (commit 79e30dbb8a, behind trainingStepsPricing flag) and resolves base via thisTrainingDetails.baseModelType ?? 'sd15' (TrainingSelectFile.tsx:481) with the server applying the same default in createTrainingWorkflow (training.orch.ts:309); the user-reported repro jobs are Jun 19–22, postdating every existing training fix (latest trainer touches — 2b041d674f save-every clamp, 24f57b098b sd15 fallback — predate the reports and don't address base inheritance). No merged or open PR fixes the SDXL/base-inheritance defaulting; git log --all since Jun 13 on the trainer files shows only unrelated changes.
- correlations: adjacent-to #68194/#68161/#68133/#68131 — same base-model-resolution bug class, aggregated as sub-reports within this ticket; adjacent-to #68154 — sample-gen base mismatch (Anima Preview3), possibly the same base-resolution code path, not yet confirmed; adjacent-to #67754 — pre-charge confirmation screen would surface/mitigate this misconfiguration, distinct work
- SPEC goal: Make the LoRA trainer dispatch on the user's selected/source base model instead of falling back to SDXL: 'Train Further' must inherit the source LoRA's base model (Illustrious/Anima) — exposing or carrying it through rather than defaulting via baseModelType ?? 'sd15' — and standalone Illustrious/Anima trainings must not silently switch to SDXL. Confirm whether the standalone-switch (#68131/#68133) and sample-gen Preview3 mismatch (#68154) share this root cause or are separate before scoping the fix.
- SPEC done(verifier): Submitting a 'Train Further' run from a known Illustrious source LoRA (and a known Anima one) produces an orchestrator training job whose resolved ecosystem+base checkpoint matches the source (Illustrious/Anima), not generic SDXL — verified on the dispatched job payload for the repro versions (equivalent of #68194/#68161); a fresh standalone Illustrious/Anima training likewise dispatches on the selected base.
- SPEC owner: civitai trainer team (Luis Rojas — authored the continue-training feature)  autonomy: gated

### [TASK] 868k41p7d — Search returns ~100K irrelevant results for long multi-word prompts (relevance / token-OR)
- confidence: medium  age: 0d  status: to do
- verified: Confirmed the root cause in code: the Meilisearch InstantSearch client (src/components/Search/search.client.ts) and all search parsers set NO `matchingStrategy` anywhere in the repo (grep for matchingStrategy across *.ts* = 0 hits), so Meilisearch defaults to `matchingStrategy: 'last'`, which progressively drops trailing query words until results appear — exactly the OR-any-token behavior the ticket describes; no client-side result cap is configured either. No merged PR addresses long-query relevance (only #2573 'rank v1 models search by relevance, not recency', unrelated API-v1 sort). Diagnosis is sound; not reproduced live against prod Meili.
- SPEC goal: Fix on-site search so a long pasted multi-word prompt ranks by relevance to the full phrase instead of returning every single-token match. Set an explicit Meilisearch `matchingStrategy` (AND-bias / 'all' or 'frequency') on the search requests in src/components/Search/search.client.ts (and/or per-index parsers) and apply a sane result cap, rather than relying on the default 'last' strategy. Note blast radius: this affects ALL search indexes (images, models, users, etc.), and over-aggressive AND matching can yield zero-result queries — choose/tune the strategy with that tradeoff in mind.
- SPEC done(verifier): On prod search, pasting the reported long prompt (the one tied to civitai.com/images/87452555) returns a relevance-ranked result set in the low hundreds or fewer (not ~100K), the actual matching image ranks near the top, and a single shared token like 'holographic' no longer pulls the entire corpus; verify via the search UI and confirm `matchingStrategy` is now explicitly set in the request payload.
- SPEC owner: civitai frontend/search (Meilisearch InstantSearch — search.client.ts + Search parsers)  autonomy: gated

### [TASK] 868k41k2f — ModelFile.sizeKB stored as MB (~1024x too small) on manual B2 upload path
- confidence: high  age: 0d  status: in progress
- verified: Root cause verified: commit 4a0b2f5468 (PR #2729, branch fix/model-file-sizekb-mb-unit-868k41k2f) shows s3-upload.store.ts initialized item.size in KB via bytesToKB() while every consumer treats size as bytes and re-applies bytesToKB(), so a completed upload with no overwriting progress frame stored sizeKB as MB. Fix is NOT merged — origin/main HEAD is 10a89baaaf and the fix commit lives only on the PR branch. Per the latest comment the one-time backfill (~98 LORA, broadened to ~122 never-legit-small rows; TextualInversion/.zip/.bin must NOT be blind-corrected) still needs manual/admin DB write and is outstanding.
- SPEC goal: Land the unit-consistency fix for upload-time size tracking and correct the already-mis-scaled ModelFile rows so sizeKB is actually KB everywhere (page display, API, vault totals, size guards).
- SPEC done(verifier): (1) PR #2729 merged to main (origin/main contains commit 4a0b2f5468); (2) a fresh manual B2 upload of a known-size LoRA records ModelFile.sizeKB == round(actual_bytes/1024) (not /1024^2); (3) replica query for mis-scaled rows — type IN ('Model') with the B2/manual-upload signature and 0 < sizeKB < 1024 on never-legit-small types (LORA/Checkpoint/LoCon) — returns 0 rows after the backfill re-derives sizeKB from the actual B2 object size (NOT a blind *1024, which would corrupt legit tiny TextualInversion/.zip/.bin rows).
- SPEC owner: manuelurenah (Manuel Emilio) — PR author/assignee  autonomy: human-trigger

### [NEEDS-DECISION] 868k41byy — Merch store + bonus Blue Buzz code program (scoping)
- confidence: high  age: 0d  status: to do
- verified: Ticket is explicitly a scoping doc (created today, 0 comments) with four unresolved product/business open questions (tiered-vs-flat bonus, Shopify+Printful vs existing vendor, email-vs-insert-card delivery, and an unassigned payments/legal sign-off owner). Verified the cited code path exists: redeemableCode.service.ts has consumeRedeemableCode (line 243) and a metadata column, so the 'add buzz-color field + thread toAccountType:blue' work is plumbing as described — but it is gated behind product decisions, not dispatch-ready. Note: git log shows an old commit 69bc99bf4f 'Remove merch related items', i.e. merch was previously pulled, reinforcing this needs an intent call.
- correlations: adjacent-to the Yellow-Buzz variant of the same proposal (shared source doc docs/merch-buzz-code-program.md); distinct product, not a duplicate
- recommendation: Escalate to Zach — this is a product/legal scoping ticket, not a task. Decide bonus model, storefront vendor, code-delivery mechanism, and assign payments/legal owner before any dev work is dispatched.

### [NEEDS-DECISION] 868k412h2 — Audio Slider Is Not Great
- confidence: medium  age: 0d  status: to do
- verified: Confirmed the audio/volume control exists in src/components/EdgeMedia/EdgeVideoWithControls.tsx (useVideoPlayerStore: muted default true, volume 0.5, persisted) — so this is a live UX surface, not a phantom. No civitai PR or commit addresses a 'audio slider' redesign (git/PR search for audio* shows only ace-audio generation + audio-training work, nothing on the post-video volume control). Ticket created today (0 days), unassigned beyond Manuel, and its description is truncated mid-sentence ('...have an audio control at the bottom but you can') with the core complaint being subjective ('not great') and an attachment video I cannot watch.
- recommendation: Needs Zach's/design call: the ticket is a subjective UX complaint plus an inconsistency report (some video posts show an audio control, others don't), but the description is cut off and there is no spec of the desired behavior — escalate to define what the fixed control should look like / which videos should expose it before any dispatch.

### [TASK] 868k3z53w — Completed hires-fix gen workflows stuck in 'processing', then force-refunded while images stay delivered (buzz loss)
- confidence: medium  age: 1d  status: to do
- verified: Genuine, independently reproduced bug: ticket documents two repros (ClickHouse buzzTransactions for user 9631718 showing a 75-blue refund ~60min after charge on a workflow with 4/4 jobs succeeded + 8/8 blobs available, plus Freshdesk #68244 / user 12016043) both on the txt2img 'Create + Hires Fix' combined path. Searched civitai git log/code for a fix — the only hires-fix commit (84c06448d4, 2026-03-11) predates the ticket and only touches step metadata; no commit/PR addresses the processing→succeeded status transition or a delivery-verifying auto-refund, so it is NOT already fixed. Could not inspect the orchestrator service (separate repo) or live metrics, and breadth in-ticket was inconclusive (~600-800 gen-refunds/hr masks prevalence).
- SPEC goal: Fix the orchestration-side bug where a completed txt2img:hires-fix ('Create + Hires Fix') parent workflow never transitions processing→succeeded after all jobs succeed and all blobs are available — and make the ~1h stuck-workflow auto-refund verify actual blob/image delivery before refunding, so fully-delivered workflows are not force-refunded (free images) nor timed-out-and-dropped.
- SPEC done(verifier): Re-running the Create+Hires Fix path: once all jobs succeed and all output blobs are available, the parent workflow status flips to 'succeeded' (no indefinite 'processing', no held queue slot), AND no force-refund ledger entry is posted for a workflow whose images were fully delivered. Mechanically: zero buzzTransactions refund rows for completed-and-delivered hires-fix workflows in a verification window.
- SPEC owner: Koen (generation / orchestration)  autonomy: human-trigger

### [TASK] 868k3yur6 — Like/save button in model add menu shows no animation/state feedback
- confidence: medium  age: 1d  status: to do
- verified: Located the exact control: the thumbs-up Like button in the generation resource-add menu (src/components/ImageGeneration/GenerationForm/ResourceSelectModal/ResourceSelectCard.tsx:231-248), driven by isFavorite = likedModels.has(id) sourced from trpc.user.getBookmarkedModels (useResourceSelectFilters.ts:21 -> ResourceSelectModalContent.tsx:144). useToggleFavoriteMutation (resourceReview.utils.ts:306) does optimistically setData getBookmarkedModels, yet the card still shows no on-click state change per the report — so the optimistic-update path is not reaching this card's render and the button lacks any press animation. No merged PR/commit addresses this (git log + PR search since Apr 2026 show no like-feedback fix); like persistence works as reported. Genuine, reproducible cosmetic bug.
- correlations: not-related-to 868k3y3yq (reaction-count incident) — ticket explicitly disclaims it; classifier grouped on the word 'liked' only, no shared code path
- SPEC goal: Give the thumbs-up Like/save button in the generation model-add menu (ResourceSelectCard) immediate visual feedback on click — flip to the active (green/filled) state and play a press/like animation — without requiring the user to navigate to the Liked section and back. Root-cause why the existing optimistic getBookmarkedModels update is not reflecting in this card's isFavorite.
- SPEC done(verifier): In the resource-add menu, clicking the thumbs-up on an unliked model immediately renders the active state (button color green + ThumbsUpIcon filled=true, with a visible animation) before any refetch, and clicking again immediately reverts — verified via Playwright on a desktop viewport and a mobile viewport.
- SPEC owner: frontend (Briant Diehl, already assigned)  autonomy: gated


## Suppressed (FYI / STALE-close / ALREADY-DONE, one-liners)


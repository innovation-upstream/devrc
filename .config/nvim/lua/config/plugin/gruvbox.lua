-- Default options:
require("gruvbox").setup({
  italic = {
    strings = false,
    emphasis = true,
    comments = false,
    operators = false,
    folds = true,
  },
})
vim.cmd("colorscheme gruvbox")

# close-the-loop — living state / ledger

_The evolving memory the `close-the-loop` skill reads first and updates last. Last updated: 2026-07-05 (clawgate AGENT-LOOP **SOAKED + VALIDATED**: over 2026-07-03→05 the Task→Dispatch→agent→**real PR**→ready_for_review loop was exercised with ~7 real test tasks (#58–#64, deepseek, innovation-upstream/devrc — `.editorconfig`/`.gitattributes`/`scripts/README.md`/`.shellcheckrc`…), each producing a genuine PR — the loop WORKS end-to-end. Hardening shipped so soak doesn't rot: **idle-task reaper** auto-dismisses >7d-idle tasks + tears down their pods (no idle-pod buildup), **live dismiss regroups sections**, **structured transcript** shows the full agent response with tool chips. Heavy soak-driven UI polish (clawgate 0.7.59→0.7.66). Loop is proven; the open question stays ADOPTION/relevance of what's dispatched, not the mechanism. Prior: REPO CHIEF-OF-STAFF FEATURE-COMPLETE + SELF-HOSTED. Discipline: STOP building loop features, keep USING it)._

## ▶ START HERE — current state + next move
**CURRENT THREAD (2026-07-01): REPO CHIEF-OF-STAFF — the "CEO model" loop, SHIPPED + verified live end-to-end.** From Zach's ask "use the telemetry to have agents generate ideas and bring them to me (modern-CEO model)" → reframed honestly (the task-spec drafter already IS a generative arm, and its adjudication leg never closed — bottleneck is adoption/relevance, not generation) → picked a NEW, better-verified source: **scan his CODEBASES for improvement opportunities and EMAIL him proposals** (surface = email, which he reads, unlike the clawgate Tasks queue he doesn't). Built `devrc/scripts/repo-cos/` (PRs #41/#42/#43 merged; operate via the **`repo-cos` skill**):
- **Pipeline:** deterministic pre-scan (TODO/FIXME, skipped tests, `latest` tags, churn, large files — `file:line` evidence, capped per-repo so civitai can't flood) → cheap LLM synthesis (OpenRouter deepseek, survivors only, ~cents) → top-5 ranked digest **emailed** (Gmail SMTP + the mailbox app-password) by a weekly `serverMode`-gated workbench timer (Mon 08:00). Anti-slop = HARD cap + `file:line` evidence validation + CI-verifiable bias, NOT the (broad) quality bar wording.
- **🔑 THE FEEDBACK LOOP is the real close — and it took a LIVE failure to get right.** v1 = context-injection (reply → inject into prompt). It FAILED live: Zach replied "these 5 are paused / not mine" and the model RE-PROPOSED them. Lesson (again): **context/prose steering is too weak; deterministic wins.** Rebuilt as **`exclusions.py`**: reply positionally → map to the repo (against `last_emailed.json` = the digest he SAW, not the rotated `latest.json`) → **HARD-DROP that repo from the scan** (permanent for "not owner"; until `resume` for "paused"). Verified live twice: paused repos now deterministically vanish. Also caught live: a #5 mis-map (emailed digest predated persistence) + a dup-key bug (basename vs path) — both fixed.
- **🔑 WHY THIS ONE HAS A CHANCE (vs the drafter that never closed):** the surface is EMAIL (he reads it), and the near-term verifier is cheap + artifact-based — the proposals themselves are actionable (first run already flagged unpinned prod `latest` tags + a maybe-unauthed endpoint), and a greenlit proposal → dispatch → PR is CI-verifiable. Reply-driven exclusions give him a 1-tap way to keep it relevant (the exact thing that killed the drafter's adoption).
- **▶▶ 2026-07-02 — FEATURE-COMPLETE + SELF-HOSTED (PRs #44/#45/#46, all live-verified):** (1) **Self-hosted mail** (#44): digest now SENDS via Zach's postfix relay `From: repo-cos@mail.zacx.dev` (DKIM, via prod `kubectl port-forward`) and the reply READS from his homelab Postgres `mail` table (routes `repo-cos@inbox.zacx.dev` → receiver → PG). Gmail is only where he reads/types; send+sign+reply-store are his infra. Bugs caught LIVE (not by tests): `mail-actions/llm.py` shadowing repo-cos's `llm.py` (importlib-load fix), a substring ownership gate (→ exact), and the reply cutoff reading `latest.json` (clobbered every run) instead of `last_emailed.json`. (2) **Recommendation-level dismissal** (#45): "skip THIS proposal" (dismiss by evidence `file:line`, repo stays in scope) vs "pause the repo" — the conflation Zach flagged. (3) **APPROVE → clawgate Task** (#46): "approve" POSTs the proposal to `POST /api/tasks` (durable, one-tap Dispatch) + suppress-on-success — the "act on approval" leg (repo-cos is now clawgate card-producer #3). So the reply now has THREE deterministic intents + context nuance, mapped against `last_emailed.json`. **🔑 Note the pattern: EVERY meaningful bug this arc was caught by RUNNING it live, not by the (passing) mocked tests + the code audits.** Each PR was adversarially audited AND live-verified; audits caught security 🔴s (STARTTLS, ownership gate), live runs caught wiring 🔴s (module shadow, cutoff).
- **NEXT / open (BUILD IS DONE — the move is USE + WATCH):** (a) **dispatch clawgate Task #39** (the devrc collector-tests proposal Zach approved) to close approve→implement→PR for real = the outcome/artifact verifier finishing. (b) **watch the FIRST Monday timer fire** — the systemd minimal-env reaching BOTH clusters (prod relay + homelab PG) is the one unproven bit; fails LOUD (rc=1), not silent. (c) ADOPTION over the coming Mondays is the real test. (d) still-noted-not-built: thread-match replies (`In-Reply-To`→exact digest) for reply-to-OLD-digest. (e) **ROTATE the OpenRouter key** (`~/.config/repo-cos/env` + transcripts). **🔑 DISCIPLINE: do NOT add more repo-cos features until a few real weekly cycles show it earns its place — the instrument is built, the value is in running it.**


**CURRENT THREAD (2026-06-30 LATER): PRODUCTIVITY INSTRUMENTATION — built the instrument, now USE it.** From "use the telemetry to track ongoing initiatives + progress" → shipped, all verified on both hosts: (1) **`initiative-scan` + `/initiatives`** (devrc, PRs #36/#40 merged) — cross-repo initiative+progress ledger (handoff docs + git + `activity.events` telemetry → momentum/next-step/owed); **dogfooded `/audit-pr` on it → caught a 🔴 UTC bug pre-merge** (the audit hook earning its keep on turn one). (2) **Folded it into `/standup`** (new `initiatives` scope, telemetry-OFF) — *the durable surface*: per the measured lesson that opt-in commands DON'T stick, the standup fold matters more than the `/initiatives` opt-in. (3) **`audit-pr-nudge` PostToolUse hook** (fires on `gh pr create` → Claude reflexively offers `/audit-pr`). **🔑 This is the AUTONOMOUS-TRIGGER answer to the memory's own finding** (the audit ritual was hand-typed ~51×/7d while opt-in `/audit-pr` was ignored) — a trigger that fires on its own, not a command to remember. (4) Structural: ALL slash-commands now under home-manager (both hosts lockstep), `/handoff` captures live-diagnosis-state (kills the re-diagnosis tax), espanso `:eos` end-of-session ritual. **🔑 HONEST DISCIPLINE CHECK: most of this was TOOLING/harness, not a closed value loop.** But the instrument now SURFACES real owed work it found: App Blocks **#2820** verify (OWED), get-started **flag flip** (HELD), and **sysredis fal.ai 403 = the only "active user impact" item**. **NEXT close-the-loop move: ACT on what `/initiatives` surfaced (sysredis 403 first) OR pick a genuine value loop — do NOT build more productivity tooling** (initiative-scan B/LLM-state + C/Grafana are explicitly de-prioritized as more-harness). The instrument exists; point it at value.

**CURRENT THREAD (2026-06-29/30): EMAIL AUTOMATION — the original "heavy automation" goal, SHIPPED + CLOSED end-to-end with real value.** This is the untouched thread the ledger kept queuing (mining the live `mail` table). Built **`mail-actions`** (devrc `scripts/mail-actions/`, PRs #31–35 merged; homelab-infra #80/#81 merged) — operate via the **`mailbox` skill** (updated this session), memory `selfhosted-mail-inbox` (updated). Four pieces, all VERIFIED LIVE: (1) **action-required extractor** — deterministic Stage-1 filter (drops alerts/bulk/`Feedback-ID`/operator-denylists; billing exemption rescues invoices) → LLM (`deepseek/deepseek-v4-flash`, OpenRouter, survivors only) → `mail_actions` queue; (2) **invoice→tax archiver** — PDF + JSON sidecar to MinIO `taxes-{year}-invoices` (deterministic, no LLM); (3) **thread reconcile** — one live action per thread (newer-msg supersede + owner-reply auto-close); (4) **sent-mail IMAP poller** — homelab CronJob ingesting your Gmail Sent mail so reconcile auto-closes actions when YOU reply. **🔑 LESSON (confirms + extends the QA-loops contrast): it CLOSED because every verifier is the ARTIFACT, not adoption** — extracted item checkable vs source; invoice PDFs land in the bucket; sent-reply ingest → action auto-closes (proven live: re-opened the naida action, reconcile closed it). And **deterministic-first is what makes it cheap + trustworthy**: the Stage-1 filter drops ~99% before any token spend (action-extract ≈ sub-cent/run; archiver + poller = $0), and **operator adjudication tuned the filter live 3534→6 survivors** (resend/voip/datapacket/avianca/linkedin all dispositioned with the RIGHT mechanism — denylist vs sender+subject rule vs `dismissed`-state vs billing-exemption — so future legit mail from each party still flows). **Autonomy:** archiver daily (workbench HM systemd timer) + sent-poller `*/10` (cluster CronJob) run themselves; **action-extractor stays MANUAL by operator choice** (he triggers `extract.py run` as needed — it's the only $ piece). **Honest pitfall caught + fixed live:** the sent-poller's first design did `UID FETCH 1:*` → OOMKilled; fixed with a 30-day cold-start lookback + batched fetch. **Open/next:** operator declined key-rotation (an OpenRouter key + the Gmail app-password sit in a 2026-06-29 transcript — flagged in memory); optional later: schedule the action-extractor too, thread-dedup polish, `--emit-clawgate` to push actions to phone. **Loop is CLOSED — next close-the-loop run should pick a NEW thread** (e.g. the still-pending `activity-scan --days 7` verifier from the QA-loops thread).

**CURRENT THREAD (2026-06-29): QA-AUDIT LOOPS — a loop that CLOSED with real value on day one (the contrast the ledger's been chasing).** From "use the activity telemetry to find automation opportunities" → built re-runnable UX-audit loops for **naida + vetr** (walk every funnel view → screenshot → opt-in vision-LLM-drafted UX notes Zach edits → hand to Claude → re-run). **The verifier-payoff fired immediately:** the vetr loop, run under the new Authorize.net prod gateway, surfaced a **LIVE revenue bug** — pooled-consult + servicer-booking pay-now checkout renders Stripe Elements with a null `client_secret` (empty form, can't pay; the FE was never made gateway-aware in the Stripe→authnet migration). Documented in `vetrllc/vetr-workspace` `payment-rails-paynow-checkout-bug.md`; another agent owns the fix. **🔑 LESSON (the key contrast): this loop CLOSED because its verifier is CHEAP + AUTOMATIC + the ARTIFACT ITSELF — run the walk, a broken view is self-evident in the screenshot/a failing assertion. The drafter loop (above) still hasn't closed because its verifier = Zach's adjudication = adoption-gated. Aim loops where the verifier is the artifact, not a human decision.** Also corrected the activity pipeline (browser `active_ms` was structurally broken on i3 → RETIRED → i3-derived attention; the prior handoff's "8/8 GREEN" + the "sandbox creds exist" claims were BOTH wrong on re-verify — memory-is-a-hypothesis bit twice this session). Built deterministic spinoffs too: `activity-scan.py` (automation/bottleneck report) + `dogfood-cycle`. Operate via the NEW **`ux-audit-loops`** skill (memory `qa-ux-audit-harness`). **Verifier for the loop itself:** re-run `activity-scan --days 7` in ~a week — manual vetr/naida QA browser-time should DROP if the loops are used. **NEXT (the untouched higher-leverage thread): email automation on the live `mail` table** (skill `mailbox`) — the original "heavy automation" goal from this whole arc, still not started.

**CURRENT THREAD (2026-06-24 LATER): the drafter's MISSING DURABLE QUEUE = clawgate Tasks. Decoded "most clawgate features are unused."** Operator: "most features of clawgate are unused; Suggestions + Tasks have the most potential, help me break the ice." Through the loop discipline → **the reframe: Suggestions + Tasks aren't dormant features to revive — they are the missing FRONT-END of the 10x task-spec lever already shipped (the drafter, next block).** Elicited (AskUserQuestion, 2026-06-24): **Tasks = the fleet's ADJUDICATION QUEUE** (verified specs land durable+statused → glance → approve → one-tap dispatch → self-verify); **Suggestions = the GENERATIVE arm** (session-end → drafts the next task-spec → SAME Tasks queue). One queue, two sources (drafter=inbound, Suggestions=internal).
- **🔑 VERIFIED finding (surface→verify before building):** the live drafter `route_clawgate` POSTs `type:permission` to clawgate **`/api/send`** (permission inbox) which has a **`CLAWGATE_REQUEST_TTL=5m` sweeper** → **the daily 13:00 digest EVICTS ~5 min later unless Zach is looking right then.** clawgate has **NO machine (hook-token) endpoint for a durable Task** — only the UI `POST /tasks` form. So the machine's verified output has no durable home to adjudicate from. That's *why* the queue feels invisible — a mis-routed feature, not a missing one.
- **▶ LOOP #1 — ICE-BREAKER — SHIPPED 2026-06-24 (clawgate half VERIFIED LIVE; drafter half deployed + first real run validating).** Routed drafter specs into the **durable Tasks queue** not the evicting inbox. (a) clawgate **hook-token-gated `POST /api/tasks`** (additive; reuses `notes` create; broadcasts `task.changed`; does NOT touch the permission flow) — clawgate **0.7.39** live; **VERIFIED: `POST /api/tasks`+token → `{"id":16}`, no-token → 401, and note 16 PERSISTS in the durable `notes` table** (the cheap deterministic verifier passed). (b) drafter `route_clawgate` (homelab `devpod-task-drafter` configmap, `kubectl apply`'d since agent-pods Flux is operator-suspended) now POSTs **one durable Task card per verified spec** to `/api/tasks` (was one ephemeral `/api/send` digest). Provenance marked in the card body (`🤖 task-drafter · {cls}`), directory=`{cls} · {ticket_id}`. First manual MODE=on run triggered (`task-drafter-loop1-193021`) to validate end-to-end. **Validate-closes bar:** ≥3 real daily runs land durable Tasks Zach actually adjudicates — soak over the coming days. **NEXT increments below (#2 dispatch-from-Task, #3 Suggestions→Task).**
- **▶ LOOP #1 CLOSED ON ITS FIRST REAL RUN (2026-06-24):** manual MODE=on drafter run processed **4 delta tickets → "SENT 4/4 Task card(s) to clawgate Tasks queue"** → **notes 17–20 PERSIST in the durable `notes` table** (provenance `🤖 task-drafter · NEEDS-DECISION · <ticket>` + safety flags). Verifier fired automatically + the cards are real + durable. Now: soak the daily 13:00 cron to ≥3 runs (does Zach adjudicate them).
- **▶ #2 SHIPPED + VERIFIED LIVE (clawgate 0.7.40, 2026-06-24): one-tap Dispatch from a Task card.** Each Task card renders a **Dispatch** button → opens the existing dispatch modal **pre-selected to that task** (`hx-get /ui/agents/new?note=<id>&label=…` → `DispatchModalBody` preselects the note combobox; FAB path byte-for-byte unchanged; reuses `createAndDispatchAgent` — no new dispatch path). Verified live: `/ui/tasks` renders 5 Dispatch buttons, each `hx-get`→`/ui/agents/new?...note=20` etc. **Validate-closes bar (still open):** Zach taps Dispatch on a real card → agent runs → opens a PR (the agent's own gate = the verifier). Needs a real dispatch to confirm. So the adjudicate→execute→self-verify chain is WIRED; the close validates when a real card gets dispatched.
- **▶ #3 SHIPPED + VERIFIED LIVE (clawgate 0.7.43, 2026-06-25): Suggestions → durable Task.** Zach green-lit building it despite the un-soaked dispatch leg. When a session-end suggestion is generated+saved (`generateAndSave`, both the auto Stop-hook path + on-demand), it now ALSO mints **one decision-shaped Task card** (`💡 next-step · from session` → 1-line ask + 2-3 grounded options + `[Open session]` scroll-back link) into the SAME `notes` queue via `s.ext.Notes.Create` + broadcasts `task.changed`. Best-effort (nil store / 0-option / Create-error → skip, never fails the suggestion); one Task per generated suggestion (auto path throttled). Standalone Suggestions tab left functional+untouched (no polish — fold-or-remove it once the queue soaks). **VERIFIED LIVE (independent re-check):** `POST /api/suggest` + on-demand generate on the open LAN pod → note 28 minted with the real OpenRouter-extracted next-steps as options; cleaned up after. Gate: go test -race + e2e **62 pass / 2 skip** + bats. **Self-verify last mile (status-flip-on-PR) NOT built — deferred.**
- **▶ DRAFTER CARD RESHAPE SHIPPED (2026-06-25, homelab task-drafter):** elicited from Zach (AskUserQuestion) = "decision-shaped: 1-line ask + 2-3 options" + fix contentless gated cards. (a) **Gated cards never contentless** (deterministic `trigger.py`): `safety_gate` preserves `original_recommendation`/`original_goal`; `_task_body`/`_title` surface the ticket's REAL ask (first non-empty of model `ask`/`spec.goal`/`original_*`/`verification`/`title`) + a `⚠ why-gated` line — never the escalation boilerplate. (b) **Decision-shaped**: SKILL.md (helmrelease) now requires `ask` (one sentence) + `options` (2-3 ticket-specific choices) in the record; `_task_body` renders `**Ask:**` + numbered `**Options:**`, with a DETERMINISTIC fallback by classification (gated → Approve intent & dispatch / Defer / Close as dup) so cards are useful even if the model omits the fields. Applied live (configmap + skills configmap patched + drafter devpod rolled) + committed to trunk. **Honest gap:** the deterministic rendering is proven (local harness); the MODEL emitting `ask`/`options` is NOT verified live (a SHADOW run's DeepSeek call ground >40min without completing — `AGENT_TIMEOUT_S=600` urllib timeout doesn't abort a streaming gateway response). Will be exercised by the next cron with a ticket delta.
- **🔴 SOAK CHECK 2026-06-29 (4 days after the gate fix) — STRUCTURE FIXED, ADOPTION DID NOT FOLLOW:** the gate narrowing + reshape WORK in production — the 06-26 daily run produced a healthy MIX incl. **4 dispatchable TASK cards** (ids 29/32/34/35) with model-emitted decision-shaped Ask + 2-3 concrete options + Done-when (card 29 = textbook: "Wire up custom Anima checkpoints…" + a 2-line-change option). **This CLOSES last session's open verification gap** — the model DOES emit good `ask`/`options` live. Daily cron healthy (later runs 8-10s = empty delta → no new cards; that's why nothing after 06-26). Zach engaged lightly: **1 card marked `complete`** (id 37, manual status change, NOT via dispatch). **BUT the dispatch leg STILL has not fired: `agent.dispatch`=0 over 4d, 0 agents ever carry a `note_id`, and `permission.action` dropped to 1/4d (was 37/wk) — clawgate engagement FELL.** 🔑 **Conclusion: every STRUCTURAL blocker is now removed (durable queue, one-tap dispatch, content-rich decision-shaped cards, real dispatchable TASKs present) and the loop STILL won't close — so the remaining bottleneck is ADOPTION/behaviour, not harness.** Per the discipline: **do NOT build more.** The fork is now Zach's: dispatch one of the 4 ready TASK cards to close it once for real, OR conclude dispatch-from-card doesn't fit his flow and CUT/PARK the queue rather than keep investing.
- **🟢 ROOT CAUSE FOUND + FIXED 2026-06-29 — the cards weren't HIS.** Zach: "I think it's getting tasks that are not assigned to me." VERIFIED against live ClickUp: the drafter pulls the whole-TEAM "To Schedule" view (token = Ellie's), and **only 2/30 tickets are assigned to Zach** — the rest are Manuel/Luis/Koen/Briant/unassigned. The one card he DID act on (marked `complete`, id 37 = cert key-gen 868k49796) **was his.** So the non-engagement was NOT "dispatch doesn't fit his flow" — it was a relevance/noise problem: ~90% of cards were other people's work he can't action. **Fix (his call: "assigned to me only"):** added `DRAFTER_ASSIGNEE_ID` filter (default 81593871) in `trigger.py` right after fetch — the view list already carries `assignees` so zero extra API calls; keeps only his tickets (2/30 now). Deployed live (configmap applied + trunk `db863467`); compiled + simulated against the live view (2/30). Cleaned the queue to his tickets only: now just **id 35 (open TASK — "fix the Cloudflare WAF false-positive blocking a Ukrainian user", HIS, dispatchable)** + id 37 (his, complete). **This is the real adoption unblock — the queue is finally his-only with a genuine dispatchable card. NOW watch whether he dispatches #35.** Lesson: when an adoption loop won't close, check RELEVANCE of the items before concluding the workflow is wrong. (Prior finding, 2026-06-25:) the loop has **NEVER closed once.** clawgate pg: notes are all `open`, **0 ever moved off `open`, 0 agents ever had a `note_id`**; Loki: **`agent.dispatch` = 0 in 7d** (vs `permission.action` = 37 — Zach uses clawgate, just for permission approvals). So **#2's dispatch→agent→PR verifier is STILL unexercised.** Root cause discovered: today's whole batch (6 cards) all hit the deterministic safety gate → all `NEEDS-DECISION` "Do NOT auto-dispatch." **The #3 build + card reshape improve ADJUDICATION quality but do NOT change dispatchability** — a dispatchable TASK card needs a non-risk-class ticket in the delta. **The dispatch leg can only soak when a genuine TASK (not gated) card appears AND Zach taps Dispatch.** Watch for that; consider whether the gate regex is over-broad (`prod|migration|delete|auth|token|secret` catch many benign eng tickets) if ~all cards stay gated.
- **▶ GATE NARROWED (2026-06-25, homelab task-drafter, live + on trunk `c5ad16b5`):** the single-keyword gate fired on **21/30 live tickets (70%)** — `\.red` matched the civitai.red DOMAIN, `buzz` matched currency-DISPLAY bugs, bare `auth`/`token`/`drop`/`prod` matched 403s/tokenizers/drag-drop/the prod env → every card un-dispatchable, starving the loop. Replaced with a **two-tier gate**: STRONG terms gate standalone; WEAK terms (auth/token/prod/migration/drop/buzz/…) gate ONLY with an ACTION verb co-occurring. **Measured 21/30 → 11/30**, every genuinely sensitive ticket still gated (cert key-gen, refunds, double-charges, merch+buzz payment program, admin force-logout). Validated: edge-case harness (9 must-gate/8 must-not-gate, all pass) + live-queue replay. **⚠️ CAVEAT — delta-scoping:** the drafter only reprocesses NEW/CHANGED tickets, so the ~10 freed tickets (already in `processed.json`) WON'T re-surface as dispatchable TASK cards until they change OR the state is reset. So the queue won't show dispatchable cards from the new gate until ticket churn / a forced reprocess (slow — DeepSeek ~tens-of-min/ticket). Next automated run: **tomorrow 18:00 UTC** (cron `0 13 * * *` America/Winnipeg).
- **GATE (honest):** Tasks-as-queue CLOSES (durable rows → dispatch→PR). "Suggest next step" standalone FAILS (verifier = Zach's judgment = workslop) → which is why #3 folds it in, not revives it. Do NOT build more Suggestions polish (full-transcript scroll-back / Web Push) — fold or cut.
- **NEXT:** build Loop #1 (subagent + tests + clawgate deploy → then the drafter route change in homelab-infra). Operator green-lit the direction. Also shipped this session: clawgate **0.7.37** (removed human session/token auth — Authelia/passkey fronts it; LAN open) + **0.7.38** (toast dedupe/cap + auto-approve height-cap). Authelia **4.39 + passkeys** at `login.zacx.dev` now fronts clawgate.zacx.dev (memory `authelia-passkey-sso`).

**CURRENT THREAD (2026-06-24): the 10x reframe + the task-spec drafter.** The arc: execution is ALREADY autonomous (clawgate auto-approve) → so Zach's bottleneck is **NEW-TASK SPECIFICATION, not execution** → 10x = a drafter that autonomously VERIFIES + triages inbound into decision-ready cards (he adjudicates, doesn't author). **SHIPPED + VERIFIED + GRADUATED TO LIVE (2026-06-24): the deep-context task-spec drafter is a kubeclaw OpenClaw agent on the HOMELAB cluster, DeepSeek V4 Pro** (off Zach's Claude subscription + off civitai prod), producing Opus-tier cards (real file:line/commit citations, ticket correlation, and the judgment Haiku lacked — self-chooses NEEDS-DECISION + flags risk). Location: `ZacxDev/homelab-infra` → `clusters/homelab/apps/agent-pods/task-drafter/`. Daily 13:00 Winnipeg CronJob → ClickUp "To Schedule" → delta-scope → agent VERIFY (git over mounted civitai/civitai + ClickUp) → deterministic `safety_gate()` → routes ONE digest card/run to clawgate (`CLAWGATE_MODE=on`).

**▶▶ GRADUATED 2026-06-24 (this session) — shadow→LIVE is DONE + VERIFIED.** Live cluster patched: `CLAWGATE_HOOK_TOKEN` added to secret `task-drafter-agent-secrets` + `CLAWGATE_MODE` flipped shadow→on on cronjob `task-drafter-trigger`. **Send path verified end-to-end** (homelab pod → clawgate `192.168.50.250:30302/api/send` → HTTP 200 + requestId; a labeled "GRADUATION TEST" card landed in the queue). Nothing auto-fires until the daily 13:00 Winnipeg cron. **Cost MEASURED (n=9 live tickets, the first real DeepSeek full pass): $0.027/ticket** (median $0.026, range $0.010–$0.039) → full 25-ticket run ≈ $0.68, daily delta (~3–5 tickets) ≈ $0.11/day (~$3/mo). Validates the DeepSeek bet: ~5× cheaper than the Haiku prototype ($0.143), ~21× cheaper than Opus ($0.58), with Opus-tier judgment. **`gh` PR-search ALREADY WORKS in-pod** (gh 2.93.0, authed as `CivitaiDevOpsAgent`, `gh search prs --repo civitai/civitai` returns results) — the wiring item is DONE; remaining is a prompt-tuning nudge so VERIFY actually invokes it (cards cited git-log, not gh PR-search). **Haiku prototype RETIRED** (`devrc/scripts/task-spec-drafter/` — systemd `.timer` disabled+unlinked so it no longer double-triages on Zach's subscription; scripts remain on disk, re-link from repo to revert).
- **FINDING — shadow cards are NOT persisted** (only `processed.json` on the PVC; cards live in rolling trajectory logs and vanish). So "accumulate a shadow week then read them" never actually accumulated anything reviewable — **graduation = the review surface** (clawgate). This is *why* graduating beat waiting.
- **Backlog SURFACED LIVE 2026-06-24 (Zach chose "do 1").** Reset `/state/processed.json`→`{}` + ran a live MODE=on job → **digest SENT to clawgate: 2 genuine TASK, 22 needs-decision, 1 verify (of 25 processed)**, gate fired 20/25, 1 parse failure, 47 baselined. After this, state is fully baselined again → back to clean TRICKLE (only NEW/CHANGED surface from the daily 13:00 cron). **⚠ GOTCHA (cost a wasted run): the cronjob's `activeDeadlineSeconds=3600` (1h) is fine for daily DELTA but TOO SHORT for a 25-ticket backlog (~2.5h; worst case 25×AGENT_TIMEOUT_S=10min). The first backlog job hit DeadlineExceeded, was killed mid-run, pod+logs deleted, and since `route_clawgate` POSTs only at the very END, nothing was sent.** To re-run a backlog: reset state → `create job --from=cronjob/...` → **immediately `kubectl patch job <name> -p '{"spec":{"activeDeadlineSeconds":18000}}'`** (mutable on a running Job) → watch for `SENT to clawgate`. Old state backed up on PVC as `processed.json.bak-*`. The hook token is WRITE-ONLY (can POST `/api/send`; reading `/api/requests` is 401 — needs Zach's session) → verify sends via the trigger log `SENT to clawgate`, not by reading the queue.
- **DURABILITY follow-up (low-risk, deferred):** the graduation is a LIVE kubectl edit; it is NOT yet mirrored to homelab-infra trunk (cronjob `CLAWGATE_MODE` + the SOPS secret's `CLAWGATE_HOOK_TOKEN`). Safe because the `agent-pods` Flux Kustomization is operator-SUSPENDED (live edits aren't reconciled away). If Flux resumes agent-pods, it would revert MODE→shadow + drop the token → re-apply or commit trunk first. Local `homelab-infra` is 498 commits behind origin/trunk + SOPS is fragile (handoff note) → didn't hand-edit; commit when convenient.
- **NEXT:** soak on real daily runs (read clawgate digests over the week — does Zach approve the cards / does pre-verification save time); tune the OVER-FIRING gate (≈9/10 → NEEDS-DECISION on incidental keywords) to recover auto-dispatch on the genuinely-safe class — only if the soak shows it matters; optional backlog live-pass (above). **Full session detail: `devrc/claudedocs/handoff-task-spec-drafter-2026-06-24.md` + the drafter ledger rows below.**

**Also shipped this arc (the infra focus-shield — funds the above by keeping infra off Zach's plate):** ADS alert auto-investigation (PR #162/#163 merged + verified on REAL traffic; allowlist = recurring DP alerts); auto-audit-on-push hook (devrc, shadow). **Open loose ends:** A-1 spine-controller crashloop (Flux auto-shipped broken v2.17.3; whole v2.17.x line broken — Kiota bug in `civitai/civitai-spine-controller`; handoff ready: fix-forward v2.17.4 OR constrain semver; needs the source-repo owner) + the systemic **image-automation-has-no-health-gate** hole; homelab findings (`openebs-hostpath` provisioner broken → used local-path; `.sops.yaml` had 10 rules stripped pre-session).

**Separate system shipped 2026-06-24 (the session-mining → personal-telemetry thread, NOT the drafter):** the **activity-telemetry pipeline** — 5 sources (zsh/tmux/X11-keylogger/Chrome/Claude-sessions) → per-host collector → dedicated authed homelab ClickHouse `activity.events` → Grafana "Activity & Productivity" dashboard + a deterministic validation harness. LIVE + verified end-to-end. Operate via the **`activity` skill**; full state in memory `activity-telemetry-pipeline` + `devrc/claudedocs/handoff-activity-telemetry-2026-06-24.md`. It answers "where does my time go" — a measurement layer, NOT a closed value-loop (mining describes the present; it doesn't pick the next altitude). Watch it doesn't become a well-instrumented saw-sharpen.

**⤵ Everything below this block is BACKGROUND / history** (the funnel-visibility chain, the reconciler-model canon, the App Blocks delegation menu, durability sweeps) — still valid as reference, but the CURRENT work is the drafter above. App Blocks EXECUTION is driven in OTHER sessions (don't duplicate); this thread's lane is the productivity machine + focus-shield.

---
_Historical (pre-2026-06-24):_ **The discipline is validated and running.** Three run types proven (A data/infra · B self/product · C outcome-verify), the run-type taxonomy is CANON, and one full chain shipped end-to-end: the **civitai growth funnel** — flying-blind → made visible → #1 leak found (**activation→paid, ~103K unpaid new users/mo, 97.8% drop**) → two quantified levers (push first-generation; day-2 return) → routed to a private team doc (`datapacket-talos/claudedocs/funnel-visibility-snapshot-2026-06-08.md`). **That chain is complete and handed to product** (shipping the levers is Zach/team's experiment; verifier = activated→paid rate).

**▶▶ DONE (2026-06-09): the reconciler-model EXPLORATION — result is CANON below (see "The reconciler model").** Held hard for the *back half* (reconcile toward a declared goal), broke productively for the *front half* (decide the goal / move an adversarial metric), and the break line falls exactly on **judgment-vs-execution = the north-star**. Two keepers: (1) the gate now SPLITS into two sub-conditions — *reconcilable iff (a) desired state is a cheap machine-checkable predicate over observable state AND (b) the actuator reliably moves observed→desired*; fail (a)→manual tool (perf-deep-dive), fail (b)→**experiment not reconciler** (funnel intervention) — this distinguishes two failure modes the old single gate lumped together; (2) a concrete **productization pipeline**: author-spec (interview) → observe+diff script (=the verifier) → known-actions runbook → action-admission gate retired by acceptance-rate ≥90% → residual escalates to agent → resolutions write back to the spec. **NEXT MOVE: pick a chain and run the pipeline on it** — the durability guard is the ready candidate (its exceptions list already IS a declared spec; just needs the observe+diff script + the write-back), gated on operator's "not yet" for `datapacket-talos`.

**Other open moves (deferred to after / alongside the exploration):**
1. **Run F (the meta-run / interview) — ✅ RUN 2026-06-18. Result = the north-star is now CONCRETE + the strategy reframed.** Recency-inverted interview (main context). Answers: **(90-day win)** App Blocks has widened to a **full app platform, launching in weeks**; last steps = **first-party apps + CLI + better dev-exp**; post-launch = **monetize + use as the proving ground for experiments that drive generation/training demand & conversion.** **(The leak)** app-platform work *gets sidelined whenever an infra issue grabs his attention.* **(His irreplaceable edge)** **deciding + the quality bar.** → **STRATEGIC REFRAME:** the infra-automation we shipped (ADS etc.) is NOT "better infra lead" — it's **focus-protection for App Blocks** (it removes the interruptions that sideline the platform); altitude-1 funds altitude-2 directly, same story. **NEW AIM for the machine: aim agents at App Blocks EXECUTION** (first-party apps / CLI / dev-exp build-out), surfacing only decisions + quality-gates to Zach (his edge — never automate those). Infra loops keep running as the focus-shield. **DELEGATION MENU produced 2026-06-18** (read-only scoping run): App Blocks substrate is FAR along (SDKs `@civitai/app-sdk@0.9.0`+`blocks-react@0.6.0` published, 3 dog-food apps live on prod, money-path proven, W11 audit 0crit/0high). **Launch blocker is NOT a flag flip** (flipping Flipt alone ships a broken marketplace — 3-layer mod-belt) — a *valuable* launch needs: Buzz payout back-half (authors get paid) + compelling first-party apps + CLI/DX finish. **Top-3 AGENT-EXECUTABLE now:** (1) finish CLI submit loop (endpoint exists in stale worktree branch `zach/app-blocks-cli-submit-api`, NO PR, ~1125 deletions vs main = needs reconciliation first), (2) flow-B' page-Buzz attribution (bounded 6-edit set, behind flag), (3) manifest config-as-code + fix JSON schema (drop required `iframe.src`) = the #1 DX complaint. **8 ZACH-ONLY decisions queued:** canonical CLI (Go vs npm — both bind `civitai`), rate-card %s (load-bearing $), Buzz-GA (does opening App Blocks open block spend? = launch fulcrum), spend-share-pays-authors?, renewals-pay?, launch flagship apps + quality bar, muted-user posture, discovery-flip urgency. Detail: `datapacket-talos/claudedocs/app-blocks-launch-readiness-2026-06-16.md` + `app-blocks-attribution-backhalf-scope-2026-06-18.md`. **BOUNDARY (operator, 2026-06-18): App Blocks EXECUTION (Go binary CLI = canonical, + the build items) is already being driven in OTHER sessions — do NOT duplicate/collide.** So THIS thread's lane is NOT App Blocks build-out; it's the **focus-shield**: infra-interruption-absorption (ADS + the toil-elimination machine) that keeps infra from sidelining the App-Blocks sessions. Next-level for this thread = make the shield SELF-RUNNING (schedule toil-scan + keep infra loops absorbing), then step back. The delegation menu above stays as reference for whoever drives App Blocks execution.
2. **Acquisition-channel run** — ✅ **RAN 2026-06-17 (Type-A, read-only); result was a productive NEGATIVE — see ledger row.** The "newly feasible" unlock was partly false: `landingPage` is the on-site entry PATH, not the external referrer; only 4.7% of regs carry any UTM (≈95% ChatGPT) and plausible.* (the table with `referrer_source`) is dead since 2025-10-23. So external channel attribution is **narrowed, not closed** — do NOT build a channel-optimization loop on this data. Two decision-grade keepers surfaced anyway (ChatGPT = silent 4th-biggest signup source @4.5%, free + single-point risk; model-page entry = 27% of signups, best activation/worst paid = the high-volume/low-value leak made channel-visible). Routing to the private snapshot doc = operator's call (NOT auto-routed).
3. **Productize the pattern** — per the "recurring monitoring is a script, the agent is for discovery" lesson: turn a validated chain into a **deterministic rail that escalates to an agent only on anomaly** (e.g. the funnel/growth pulse + experiment-verifier as a cron script; agent budget reserved for the novel question). The path from "ran it by hand" → "runs itself."
4. **Partly SHIPPED / parked:** the durability monitoring DID ship as deterministic in-cluster sweeps — **`dr-sweep`** (backup-freshness guard, LIVE 2026-06-09; `DrSweepStale`/`DrBackupStale`/`DrOffsiteMissing`) + **`cert-sweep`** (TLS-cert lifecycle, LIVE 2026-06-15) — the "deterministic rail" architecture realized, each with its own `*Stale` PrometheusRule. Still parked: the *context-capturing* refinement (records operator exceptions → converges to only-real-gaps) and the `nats` JetStream real-gap fix (operator "not yet" on prod execution — UNVERIFIED whether nats now has a backup). Plus the decision-labeling **write-back** (now a **clawgate-app feature** per the spin-out; build it there).

**Boundary:** clawgate *app* dev was spun out to `homelab-talos/containers/clawgate/session-spinout-2026-06-08.md` — this thread doesn't touch it except depending on the write-back (a clawgate feature). The `SKILL.md` process is stable; consolidation lives here in STATE.

## ⚡ THE 10x REFRAME (operator 2026-06-23 — "automating the audit saves seconds, useless; I want to 10x my productivity; think bigger")
Toil-elimination is SUBTRACTION → caps at ~1.2x (zeroing all his typing ≈ 1hr/day). 10x needs a MULTIPLIER change. **The multiplier is PARALLELISM, and it's pinned at ~1–2 because he runs agents like a craftsman — one hand-driven session at a time (kick off → steer → review → decide for each).** His output = judgment × (#agents he can personally drive); the 2nd term is the ceiling, NOT his overhead. **10x = shift Zach from OPERATOR (drives each agent) → DIRECTOR (declares intent for a fleet that self-runs; adjudicates only exceptions).** This is literally the reconciler-UX vision already in this doc ("you stop running loops, you declare specs + adjudicate exceptions") + the clawgate harness he already built (dispatch/checkpoints-to-phone) — **he just isn't living in it; he hand-drives instead.** The gap between the harness he has and how he actually works IS the 10x. **Four binding gaps keeping him operating not directing:** (1) agents don't self-drive toward a goal (he starts+steers each), (2) no single command surface (he context-switches across sessions to see what each needs), (3) no graduated trust (everything waits for his approval, incl. proven-safe classes), (4) decisions arrive RAW (he does the legwork before he can judge). Kill those → parallelism 2→20 on the same hours. **Do NOT pursue more toil-removal/seconds-savers. The project is re-architecting his ROLE.** Note: don't do project work (App Blocks etc. have dedicated agents); the lever is the FLEET-DIRECTION layer over those agents.

### ⚡⚡ BOTTLENECK PINNED (operator 2026-06-23, the decisive clarification): EXECUTION + APPROVAL ARE ALREADY SOLVED via **clawgate auto-approve** — most tasks already run fully autonomously. **The bottleneck has shifted UPSTREAM to NEW-TASK SPECIFICATION.** 10x = 10x the rate of well-specified tasks flowing INTO the fleet. (So the operator→director experiment is moot — he's already a director; the constraint is feeding the fleet.) **Tasks come from 2 sources:**
- **EXTERNAL (inbound signals → must be triaged + spec'd):** emails, ClickUp, Discord, Grafana alerts. Coverage today: Grafana→**ADS** ✓ (alert→auto-investigate→draft-fix), ClickUp→**standup-triage** ◐ (propose-only triage), **email→✗, Discord→✗, no unified queue.**
- **INTERNAL (generative):** original ideas + **Claude-generated** ("analyze X then propose…"). Coverage: the **question-generation loop is VALIDATED** but runs ad-hoc, not continuously; "analyze X→propose" is still hand-typed.

**THE 10x LEVER: shift Zach from AUTHOR of task-specs → ADJUDICATOR of DRAFTED specs.** A layer that (a) watches external sources + (b) runs internal idea-generation, **drafts ready-to-dispatch task-specs** (goal/done/verifier/which-agent), and feeds ONE queue → auto-dispatch trusted classes / one-tap-approve the rest. Adjudicating a drafted spec ≈ 10x faster than authoring one. **Precedent already in his fleet** (ADS drafts from alerts; standup-triage from ClickUp) — generalize "draft-the-spec, Zach-adjudicates" across ALL sources + unify the queue. **Binding constraint to test = DRAFT QUALITY** (a spec is only leverage if adjudicating beats rewriting). First proof = an adjudication-ratio experiment on ONE source (below).

### PROOF RUN + DEEP-CONTEXT VALIDATION (2026-06-23)
**Naive drafter** (titles/metadata) on a real batch (ClickUp + Gmail + 183 Alertmanager alerts): correctly filtered the large majority as FYI/noise, but on the 11 drafted TASKs the operator's adjudication = **6 approve / 2 reject / 3 stale-check**. Every miss was a MISSING-CONTEXT error, split by source: **live alerts drafted 4/4 (signal = state); stateful tickets misfired.** Killer case: naive CU-5 "add meili backup cron" **would have CRASHED Meilisearch** (it's intentionally suspended). Lesson: never auto-dispatch a stateful-source spec without reading its state; alerts are safe (signal IS state).
**Deep-context drafter v1** (ENRICH full body+comments+status+age → VERIFY vs live git/PRs/metrics/config → CORRELATE on verified links only → classify → draft only genuine TASKs; safety rule: can't-verify-intent → FLAG not draft) re-run on the same 8 tickets: **8/8 correctly classified vs naive ~2/8.** Caught CU-5-suspended (with the documented 494GB-orphan reason), CU-1-already-fixed (PR #2374 merged 3wk ago, no latency alert), CU-6-stale (verified live DNS NXDOMAIN), CU-7-verify (PR #2437 still open), CU-3/CU-8-underspecified→flag, and found CU-4 is ADJACENT-not-duplicate of CU-1 (corrected the prior assumption). **THE REFRAME:** of 8 inbound "tickets" only **1** was a genuine dispatch-ready task — the rest dissolved on verification (done/stale/underspecified/deliberately-off). **So the 10x is NOT "draft more tasks faster" — it's autonomous VERIFICATION that collapses noisy inbound to the genuine few, decision-ready, AND prevents harmful misfires. The drafter is a verifier/bullshit-filter as much as a drafter.** Value = Zach stops processing raw inbound (read→cross-check→decide per signal); he glances at a small pre-verified queue. **VALIDATED. Next build = make the deep-context drafter CONTINUOUS across all sources → gated adjudication queue / auto-dispatch trusted (alerts).**

### DRAFTER v1 BUILT + PROVEN (2026-06-23) — `devrc/scripts/task-spec-drafter/` (worktree branch `worktree-agent-ae133833d1e50ab1e`, commit 621f9e7, NOT pushed)
Shadow-first, clawgate-integrated (shadow), daily systemd timer, README + graduation path. Pipeline runs via headless `claude -p` with a **read-only tool allowlist** (KEY fix: `--permission-mode plan` blocks tools → model reasons from title only = the failure mode; the allowlist makes verification real). Proof on live ClickUp (8 tickets): 8/8 classified, citations real, **safety rule fired correctly twice** (refused harmful confident drafts: TLS-on-.red, Blue-Buzz currency program), clawgate shadow validated, zero writes. **THE BINDING CAVEAT = COST: ~1.7min Opus + tool calls PER TICKET = dollars not cents; full ~76-ticket queue is materially expensive (standup-triage runs ~$0.04/day).** Also: nondeterministic on the TASK↔NEEDS-DECISION boundary (track ratio over runs); no dedup/write-back yet; headless host needs `loginctl enable-linger`. **NEXT (recommended): push branch → ONE measured full-queue shadow run → get real cost + adjudication ratio → if ratio strong + cheaper-model brings cost in range, schedule daily + graduate alerts to auto-dispatch; else keep on-demand for high-value subset. Don't flip `on`/schedule until that measured run.**

### A-1 fix — STOPPED CORRECTLY; the prior diagnosis was INVERTED (verify-first prevented a prod incident — the thesis proven live)
Re-verify found the crashing **v2.17.3 RSs are the CURRENT/desired revision** (a live code bug: Kiota serializer unregistered in `WorkerRegistrationManager.cs:589` → StopHost loop, ~250 restarts/21h); the **healthy v2.16.15 pods are the OLD revision still serving** (maxUnavailable kept them up → degraded, NOT down). So "delete old crashing RS" (the approved instruction) would've been futile (recreated) or an OUTAGE (deleted the only working pods). Subagent hit STOP, mutated nothing. Fix = rollback to v2.16.x OR patch the Kiota bug — a CODE/RELEASE decision (spine-controller owner's lane), GitOps-entangled (manual `rollout undo` likely re-applied by Flux unless source reverted). **Recommended: route to owner; first do a 2-min Flux-managed check — if NOT reconciled, a gated rollback sticks → do it; else handoff.** Degraded-not-down so no rush. LESSON (again): verify don't assume — even an "approved gated fix" was built on an inverted diagnosis; the verify-first + STOP-condition design is what caught it.

### DRAFTER LANDED + MEASURED + A-1 RESOLVED (2026-06-23, later)
**Drafter LANDED** on devrc `main` (`8cdffb1`, pushed to origin innovation-upstream/devrc; cherry-picked paths-only, dirty files untouched). **MEASURED (n=20 Opus, live queue=74):** adjudication ratio = 5 TASK / 11 NEEDS-DECISION / 4 VERIFY = **only 25% genuine dispatch-ready, 75% dissolves** (the value), 9/20 safety-flagged. **COST: Opus $0.58/ticket → ~$43/day full-queue ($1,280/mo) = too expensive daily. CHEAP MODELS BREAK SAFETY** — Haiku & Sonnet both confidently mis-drafted the safety-critical `.red` ticket as high-confidence TASK w/ no flag (the exact misfire the drafter prevents); they run tools but lack judgment. **→ THE VIABLE MODEL: Opus on the DELTA only** (new/changed tickets since last run, ~handful/day = a few $/day), NOT full-queue-daily, NOT cheap-model. Re-scope to delta-drafter → shadow a week → gather real ratio → graduate. (Don't cheap out on judgment; cheap out on scope.)
**A-1 RESOLVED = Flux-managed handoff, no mutation.** Confirmed Flux image-automation auto-bumped v2.17.2→v2.17.3 (semver `>=1.0.0` → manual rollback gets re-bumped). Handoff ready (exact files `clusters/production/apps/spine-controller/{stable,next}/kustomization.yaml` lines pinning the tag + imagepolicy/automation; fix A = patch Kiota serializer registration → cut v2.17.4 → Flux auto-rolls; fix B = constrain semver to hold v2.16.x). Bug in separate repo `civitai/civitai-spine-controller` (Tekton-built); owner = that pipeline's owner (unknown human). **SYSTEMIC HOLE (bigger than the incident): Flux image-automation auto-deploys broken releases to prod with NO health gate — the whole v2.17.x line is broken (earlier bumps also never completed) and it'll keep shipping broken tags.** Candidate loop: a deploy-health gate on image-automation (kin to gitops-gate/dependency-pr-auditor). **Open: who owns civitai/civitai-spine-controller; whether to scope the image-automation-no-gate hole.**

### HAIKU SWITCH + SAFETY GATE — committed + measured (2026-06-23, operator: "use haiku not opus")
Committed `43a8a3a` (devrc main): drafter defaults to **Haiku** + a **deterministic `safety_gate()`** (regex over ticket text + model output for security/money/destructive → force NEEDS-DECISION, blank spec, needs-Zach, audit trail; overrides the model — does NOT trust Haiku's self-assessment). **Measured (10-ticket shadow):** $0.143/ticket (**4.1× cheaper** than Opus; full-queue $10.57, delta-only ~$0.71/day). **Gate PROVEN load-bearing:** Haiku DID mis-draft the `.red` cert ticket as TASK → gate force-escalated to NEEDS-DECISION[security] (9/10 fired; the 1 clean TASK didn't). Verification quality real (git/PR/pod/Freshdesk cross-checks). **Verdict: good enough for the shadow drafter** — Haiku does the legwork, the gate guarantees safety. **KEY CAVEAT: the gate over-fires (9/10, incidental keywords like a refund *mention*) → almost everything → NEEDS-DECISION → the win is FASTER decisions (pre-verified glance), NOT fewer (auto-dispatch path ~empty for tickets). Tightening gate false-positives = the refinement to recover auto-dispatch volume, only if the shadow week shows it matters.** Haiku over-drafts (7/10 raw TASK) so the gate is essential. **NEXT: wire delta-scoping (new/changed only) + enable daily shadow schedule (`loginctl enable-linger`) → accumulate the real adjudication ratio over ~1wk → graduate/tune.**

### ✅ DRAFTER NOW LIVE IN SHADOW + SELF-RUNNING (2026-06-23, commit `c184890`)
Delta-scoping built + proven: state file `~/.claude/task-spec-drafter/processed.json` (ticket→date_updated); processes only NEW/CHANGED, skips unchanged (proof: 1st run 25 + baselined 48; immediate 2nd run processed 0/skipped 72/zero model calls). First-run backstop caps at 25 + baselines the rest (so the ~74 backlog is marked "seen," only re-triaged on change). Daily user systemd timer ENABLED (linger on), next fire 2026-06-24 09:16 CDT, **SHADOW (Haiku+gate, clawgate logged-not-sent)**. Read the accumulating queue: `~/.claude/task-spec-drafter/latest.{md,jsonl}` + per-day `queue-<ts>.*` + `drafter.log`. Disable: `systemctl --user disable --now task-spec-drafter.timer`. **CAVEAT: first run baselined 48 backlog tickets WITHOUT classifying them → go-forward only catches CHANGES; a genuine backlog task won't surface until it changes. Optional one-time full-backlog pass (~$7/48 tickets) to triage the existing backlog (mostly stale-closes + a few hidden tasks).** **THE 10x MECHANISM IS NOW RUNNING — nothing more to build; it accumulates. NEXT CHECKPOINT ≈1wk: read the adjudication ratio (does Zach approve its cards / does pre-verification save time) → graduate (tighten gate to recover auto-dispatch on safe class) or adjust.** Productionization (declarative home-manager systemd.user units) deferred until proven. [[productivity-command-suite]] superseded by this autonomous-loop direction (opt-in commands didn't stick; autonomous did).

### CTL RUN 2026-06-23 (devrc tmux/espanso session) — 5 candidate loops, ALL REJECTED (ship nothing)
A productivity-tooling session (extended tmux scratch slots 6→12; mined 8,984 msgs + tuned espanso prompts, PRs #4/#5) ran close-the-loop to "pick the first loop to ship." Candidates: (1) `/ship` one-command two-host home-manager converge + structural verifier, (2) STATE.md → ranked next-actions backlog for `:wn`/`:rns`, (3) scheduled gh PR/CI/comment morning brief, (4) auto-audit gated PRs, (5) close graphical/sudo verify gaps. **Verdict: ship none.** Each is the recency-biased-toil-mining anti-pattern (automate what we just did this session) and/or already-shipped/opt-in: **#1** passes the mechanical gate (bounded deploy + empty-diff verifier) but FAILS aim — pure toil-subtraction (~1.2x cap), infra-hygiene quadrant (3+ hops from value), opt-in command (validated: don't stick), and it's the AGENT's minutes not Zach's judgment; **#2** has no automatic verifier ("right next step" = human judgment = workslop) AND is redundant with the task-spec-drafter (the drafter IS the decision-ready queue); **#4** is already built (`auto-audit-on-push`, ledger below) — my version was a worse opt-in twin; **#5** low-value, partly irreducible. Only **#3** touches the validated aim (new external source → unified queue) but belongs as a drafter SOURCE-extension (GitHub PRs/CI), is partly covered already (standup-triage / dependency-pr-auditor / pr-reviewer), and is gated on the shadow-week measurement (don't add sources before draft-quality is proven). **The real next move is already on the board: let the task-spec-drafter shadow week run → read the adjudication ratio at the checkpoint** (next fire 2026-06-24); building any of the five = sharpening-the-saw off the live 10x lever. Re-confirms recency-biased-toil-mining + opt-in-doesn't-stick with a fresh instance.

**↻ OPERATOR OVERRIDE (2026-06-23, same day) — verdict on #1 reversed; two lessons CORRECTED:** (1) **"opt-in doesn't stick" is about HUMAN ritual commands, NOT agent-invoked primitives.** An agent calling a deterministic `ship` (or `/audit-pr`, `/handoff`) every time is the **deterministic-rail** pattern the ledger *favors* — these earn their keep via AGENT usage even if Zach never types them. The yardstick is "is it used (human OR agent)," not "does Zach type it." So the prior "barely used → don't invest" reading of audit-pr/handoff was wrong; they're agent-load-bearing. (2) **Small compounding wins are IN-SCOPE alongside the 10x lever** — "think bigger / no more toil-removal" was too absolute; token-savings + consistency + agent-error-reduction stack in parallel with the task-specification 10x work, they don't compete for it. **Re-scored `/ship` as an AGENT rail:** passes the gate (bounded converge, cheap automatic verifier = both hosts' HEAD==origin/main + switch rc0 + generated-artifact match) AND now passes aim (removes the AGENT's repeated multi-step cross-host toil — measured this very session: the manual stash→pull→pop→switch→verify dance ran ~5× and was botched twice [merge-FF mess, espanso CLI-restart stopped the service]). **BUILDING IT** as `devrc/scripts/ship.sh` + a CLAUDE.md pointer so future agents default to it. Caveat: covers home-manager (user-level, most config changes) on both hosts; `sudo nixos-rebuild` (system/i3) stays a human handoff (interactive password).

### DeepSeek V4 Pro request — NOT a swap, deferred (2026-06-23, operator: "set it to deepseek v4 pro")
Model verified real + tool-capable (`deepseek/deepseek-v4-pro` → `deepseek-v4-pro-20260423`, OpenRouter). **BLOCKER: the drafter runs via `claude -p`, authed by Claude.ai OAuth → Anthropic-endpoint only; Claude Code 3P = Bedrock/Vertex/Foundry, NOT OpenRouter; no router/proxy installed.** So using DeepSeek while KEEPING the tool-use verification (the drafter's whole value) needs a real build: (1) an OpenRouter→Anthropic translation proxy (claude-code-router/LiteLLM) the `claude -p` points at, or (2) a native OpenRouter agentic tool-loop branch in the drafter. A naive chat-completion swap strips the tool loop → degrades to title-guessing. Subagent correctly changed nothing (still Haiku). **RECOMMENDATION (operator deciding): DON'T build it yet — optimizing the model before the Haiku shadow week tells us model-quality is even the bottleneck is premature. Let the ratio come in; if Haiku's cards are good enough, DeepSeek is unnecessary; if not, the proxy build is justified and `deepseek-v4-pro` id is ready.** standup-triage's DeepSeek path (OpenRouter direct, model `deepseek/deepseek-v4-flash`, key in `standup-triage-secrets`) is the creds reference if the build happens.
**⚠️ COST CORRECTION (2026-06-23): the drafter reaches Haiku via `claude -p --model haiku` authed by Zach's CLAUDE.AI OAUTH SUBSCRIPTION (`~/.claude/.credentials.json`=claudeAiOauth only, no ANTHROPIC_API_KEY, default endpoint) — NOT a metered API key.** So all the "$/ticket" figures ($0.143 Haiku, $0.58 Opus, $43/day) are the **API-LIST-PRICE EQUIVALENT** `claude -p` reports, NOT an actual bill. The REAL cost = **Zach's Claude subscription usage QUOTA** — the drafter competes with his interactive Claude Code usage + plan rate limits. **This reframes DeepSeek: its real value is ISOLATING the automated drafting OFF the Claude subscription onto a separate metered API (small actual $, preserves Claude quota for interactive work) — a sound architectural reason, not "save notional $."** For the low-volume shadow week (delta ~handful/day) the quota hit is tiny → fine on the subscription. The OpenRouter/DeepSeek build earns itself at SCALE (full-backlog pass, frequent/unattended runs, graduation to all sources). Operator deciding: build OpenRouter path now vs let cheap shadow week run on subscription first.

### ✅ DRAFTER PORTED TO KUBECLAW AGENT ON HOMELAB — DeepSeek V4 Pro, SHADOW (2026-06-23)
Operator: "use kubeclaw … deepseek v4 pro … deploy to homelab cluster." Done: the deep-context drafter now runs as an **OpenClaw kubeclaw agent on DeepSeek V4 Pro (OpenRouter)** on the **HOMELAB cluster** (`ZacxDev/homelab-infra`, `clusters/homelab/apps/agent-pods/task-drafter/`, trunk, chart 0.5.2, image `ghcr.io/zacxdev/openclaw-image:2026.6.1`). **Solves the trifecta: off Zach's Claude subscription (metered OpenRouter), off civitai prod (personal homelab), AND keeps the deep-context tool-use VERIFY (openclaw agent has real git/ClickUp tools — the value vs a tools-less classifier).** VERIFIED: pod 4/4 Running, HelmRelease Ready, read-only RBAC, **deepseek-v4-pro answers in-pod (smoke SMOKE-OK, Gotcha #4 cleared)**, shadow run fired (72 fetched, 25 delta-scoped, safety_gate post-processor, CLAWGATE_MODE=shadow sends nothing). Creds reused from civitai live cluster (standup-triage OpenRouter+ClickUp, support-agent civitai/civitai deploy key, devops GITHUB_TOKEN); SOPS w/ homelab age key. dp-1 Prometheus VERIFY axis dropped (unreachable from homelab). **CAVEAT: infra+model verified but DRAFTING OUTPUT/quality/real DeepSeek cost NOT yet confirmed — shadow run mid-flight (DeepSeek+tool-use over cold 1GB clone slow on ticket 1). Check `kubectl -n devpod-task-drafter logs job/<trigger>` after completion / tomorrow's 13:00 run.** NOTES: (1) `agent-pods` Flux Kustomization operator-SUSPENDED (~37 agents) → deployed via direct `kubectl apply` matching committed trunk manifests; Flux adopts on resume. (2) homelab finding: `openebs-hostpath` provisioner BROKEN (logs provisioned, never creates dir → PVC hang); switched to `local-path`; separate infra issue. (3) `.sops.yaml` had 10 rules silently stripped in working tree pre-session (Gotcha #18) — caught + restored additive-only. **TWO drafter impls now in shadow: devrc/Haiku/subscription (prototype) + homelab/DeepSeek/OpenRouter (production architecture) — converge on homelab once its output proves out, retire devrc/Haiku. Next: daily shadow runs accumulate → read cards → DeepSeek-vs-Haiku quality+cost decides graduation.**
**Bonus (same lesson, infra):** A-2 disk fire decomposed — it's on a SEPARATE GPU cluster `submodel-dc-04-a` (federates metrics into dp-1; not in kubeconfig/topology → unreachable to prune; needs that cluster's owner); real (−382GB/day, 12/14 nodes DiskPressure; spine-controller DaemonSet model-data + dind-cache; repo `k8s-disk-pressure` skill documents it). A-1 spine-crashloop is INDEPENDENT (code bug: Kiota serializer unregistered → StopHost loop; only OLD ReplicaSets crash = stuck v2.17.3 rollout on dp-1; fix = delete dead RS, prod-gated). A-4 mostly noise (46/55 normal dp-1 cron failures). "4 correlated fires" was wrong — verify don't assume.

## North-star (the goal everything serves)
Zach is **infra lead** for `datapacket-talos` (civit production) + `civitai-gpu-fleet` — his primary responsibility, now **transitioning to maintenance mode**. As that wraps, he is **shifting focus to civitai the product** (the **App Blocks** project is his first major product contribution, ships soon). **Any freed hours go to civitai product + revenue.**

His bottleneck is **judgment + deep technical focus**, NOT execution or customers: he picked "deciding what to build" + "hard technical problems" as the high-value work that gets crowded out, and "spread too thin" as the leak. So loops should either (a) **free him from infra safely** (the maintenance-mode unlock) or (b) **generate well-formed options for his judgment / absorb work that crowds out deep focus** — never try to replace the judgment.

### The deeper goal: compound the quality of the QUESTIONS, not just the answers (2026-06-07)
Zach's framing: *"determining next steps requires going to where the action is and asking the right questions — of the data and of ourselves. If we automate asking + answering better and better questions, we get better results faster."*
- **Key insight (it solves the perf-deep-dive failure):** a *well-posed question is a bounded, verifiable work-unit* — "answer THIS question from THIS data" passes the gate; "go analyze X" doesn't. **Automating question-asking is the antidote to the open-ended-analysis trap, not another instance of it.** The question is the boundary.
- This is the highest-leverage direction because it scales his *judgment* directly (the bottleneck), and it's the **front-end of this very skill** — aim-selection IS question-generation. Better questions → better loops → better results.
- It's also the loop that finally *uses* the decision-labeling substrate (0.6.4), which currently powers nothing.

## Validated lessons (do NOT relearn)
- **The gate:** a loop closes only with a **bounded work-unit + a cheap *automatic* verifier**. Proven by `perf-deep-dive` failing **2/2 real runs** (context overflow; silent model timeout) — open-ended LLM analysis + human-judgment verifier does not converge. [[clawgate-loop-validation]]
- **Infra-hygiene quadrant is drained** — cheap verifier but low revenue-proximity. Pursue infra ONLY where it's the unlock (durability/saturation → maintenance mode). Generic hygiene (drift/orphans/etc.) is not the value.
- **Instrumented-but-unread is the real gap** — Zach has Prometheus/observability and STILL lost Redis data + had nodes freeze. The missing piece is the **outcome verifier + routing**, not capability. Before building a checker, diagnose coverage-gap vs closing-gap (does a check exist whose finding went unread?). **✅ RE-VALIDATED 2026-06-16:** asked to "schedule `fleet-status.sh`" as automation — diagnosed coverage FIRST instead of building, and found staleness was ALREADY alerted for 10/20 cronjob levers (capacity/reliability×6/dr/cert/reaction-abuse `*Stale` rules) + all deployments (generic `Kube*` rules); a scheduled fleet-status would have been redundant + off-channel + lower-fidelity. The real gap was NARROW — 10 fleet cronjobs with no staleness alert (`KubeJobFailed` catches run-and-fail, not silent-stop). Closed at the right layer (a Grafana warning alert in the existing alerting stack), NOT by adding a parallel checker. The instinct to "schedule the script" is the agent-wash trap; the discipline caught it.
- **Compound engineering** — capture every correction as a rule so it can't recur. The **write-back** (corrections → agent rules) is the highest-leverage compound primitive still open — but build the CAPTURE half deliberately at the right granularity (see next bullet).
- **⛔ The decision-labeling loop was REMOVED (clawgate 0.7.18, 2026-06-11) — it captured the WRONG distribution.** It recorded every approve/deny at permission cards + checkpoints, but live data was **1496 rows / 3 days, 100% `permission`-`approve`, ZERO checkpoints** — i.e. logging Zach reflexively approving his own `kubectl`/`cd` commands across every repo, not genuine judgments. The acceptance-by-runbook table it existed for was **permanently empty**. **Durable lesson: a capture mechanism only yields signal if it captures real, scarce JUDGMENTS at the right granularity — a fire-and-forget per-turn approval is not a judgment.** The graduated-autonomy / write-back vision above must NOT be rebuilt on a "label every approval" substrate; the signal lives in *checkpoint* decisions on *runbook-dispatched* work (scarce, attributable) — which only exist once such runbooks run at volume (they don't yet).
- **STILL-unvalidated candidate loop: clawgate "Suggested next step" (0.7.23, matured 0.7.25→0.7.30).** A `Stop` hook → per-project-opt-in (throttled) OpenRouter suggestion in a Suggestions tab; right gate (cheap per-turn POST, LLM only on flagged-project/on-demand, advisory). **It's the live test of whether "suggest the next step" is signal or noise.** ⚠️ **Note (2026-06-23): it got iterated A LOT (context-from-transcript, hybrid generation, scroll-back detail view) over 0.7.25→0.7.30 — but those were Zach's bug-reports/feature-requests from using it on his phone, NOT autonomous investment.** Usage signal is therefore AMBIGUOUS: he's clearly looking at the cards (he reports issues), but whether he flags a project for auto-suggest + acts on a generated next-step is still unconfirmed. **Before building MORE (e.g. the offered full-transcript-upload for unlimited scroll-back, or Web Push): get the explicit signal — does he act on a suggestion?** If the cards are just a passive "what did it do" log (which the scroll-back detail view now serves well), that's a different, simpler product than the agentic "next step" loop. Don't let request-driven polish masquerade as validation.
- **Aim by verifier-cost × revenue/time-proximity**, and follow "where is Zach's judgment the bottleneck." Don't aim from recent-toil mining (recency-biased).
- **Recurring MONITORING is a script; the agent is for DISCOVERY/ADAPTATION + escalation — never agent-wash deterministic recurring work.** Once an agent has crystallized the queries (reconstructed a funnel, found the schema gotchas, settled the segmentation), *re-pulling those known numbers on a cadence is a cron job, not an agent* — an LLM there is pure cost + unreliability. Architecture: **deterministic rails (a static script does the recurring pulse + the experiment verifier + diff→living-issue) that ESCALATES to an agent only when it hits something it can't explain** (anomaly / threshold cross / schema break → "why?"). The agentic budget goes to discovery + the genuinely-novel question, not to monitoring. (Zach caught this when "growth-watch standing loop" was really a report script — the discipline applied to ourselves.) Same shape as the durability guard: mechanical check = cheap/deterministic, the context/judgment = where intelligence belongs.
- **AUTONOMOUS loops stick; OPT-IN command-shortcuts don't — measured at 1 week (2026-06-23).** The toil-scan re-check showed: ADS (autonomous: fires without Zach) held up + the infra-investigation toil it targets went quiet; but the productivity *commands* built the same week (`/audit-pr`, `/analyze-service`, `/ux-audit`, `/handoff`) are barely used — the audit ritual is STILL hand-typed (~51×/7d as `typed: audit/review PR`, not `cmd:/audit-pr`). Only `/find-session` stuck (0→3). Even when the audit skill ran, it was because Zach typed the natural-language sentence and the system routed it — he never reaches for the slash-command. **Lesson: "remember to type the command" is itself an input that fails (the reframe holds). The leverage layer is AUTO-TRIGGER or transparent natural-language routing, NOT a slash command the human must opt into.** Don't invest more in opt-in shortcuts; invest in autonomous triggering. (Confounds on the toil-scan deltas: last-week's window included the heavy devrc tooling session that inflated audit/verify counts; main-host-only; this-session contamination — so the *drops* are directional, not precise. The adoption *pattern* — autonomous-yes, opt-in-no — is the durable signal.) **↻ AMENDED 2026-06-23 (operator): the yardstick is "used by HUMAN *or* AGENT," not "does Zach type it." Opt-in fails for HUMAN ritual only; an AGENT-invoked deterministic rail (`ship.sh`; and `/audit-pr` + `/handoff`, which agents DO reach for) is the FAVORED pattern, not the rejected one.** Do NOT re-reject a good agent-rail by citing "opt-in doesn't stick" — that bit us this very session (the CTL run first rejected `/ship` on the stale reading; operator corrected). Small agent-rail consistency/token wins stack ALONGSIDE the 10x lever, they don't compete for it.
- **A code+config PR that doesn't bump the DEPLOYED ARTIFACT is inert on merge — "the code landed" ≠ "the change ships."** (2026-06-17, caught by the adversarial audit on PR #162.) The first cut added `gates.py`/`main.py` code + a new env var but left `deployment.yaml` pinned at the SAME image digest → Flux would have rolled a new env onto the old binary that ignores it. I'd already told the operator "shipped, just merge it" — wrong. The audit (`/audit-pr`) caught it. Durable rule: before claiming a GitOps change is ready, verify the deploy artifact carries the code (image tag **+ digest** bumped, matching the repo's per-commit convention), not just that source changed. Always run the adversarial audit on an agent-built PR before trusting/merging — it caught a 🔴 inert-on-merge + a 🟡 guard-bypass the implementer missed.
- **The capability is often ALREADY BUILT but deliberately TARGETED AWAY from the operator's actual toil — closing the loop = inverting the targeting, not building.** (2026-06-17, the reduce-Zach-input program.) Zach's #1 forced input is reactive alert investigation (`/investigate-alert` 32× + network/orch). Traced it: the full autonomous loop (Alertmanager→ADS auto-diagnose→runs the *same* `investigate-alert` skill→opens a remediation PR vs talos-infra→Zach-merge=gated approve→auto-verify-cleared) was **already LIVE** — but two gates (deviation-gate suppress + `should_auto_investigate`'s `has_context` skip) encoded the assumption *"the expert handles the KNOWN alerts faster, so only auto-handle NOVEL ones."* Zach's new metric ("every input I give is a system failure; the known recurring alerts ARE the toil") **inverts that assumption** → the close was a few-line allow-list that lets recurring-actionable alertnames through the gates, NOT new infrastructure. Generalizable: when an operator still does X manually despite automation existing, first check whether the automation was *tuned to exclude* exactly X (cost/trust-conservative defaults) before assuming a capability gap.
- **A "newly feasible" unlock must have its SIGNAL verified to exist before you build a loop on it (surface→verify applies to the *premise*, not just findings).** The acquisition-channel run (2026-06-17) was aimed because `landingPage` "became readable + carries UTM" — but the data didn't actually carry the external-referrer signal (landingPage = on-site path; UTM on 4.7% of regs, ~95% ChatGPT; the real referrer table dead 8mo). The run's highest value was the **negative result**: it stopped an over-investment in channel-optimization off data that can't support it, and named the real fix (revive plausible / capture UTM+referrer at signup). A bounded read-only run is the cheap way to falsify a premise BEFORE building — "verify the unlock is real" is itself a loop-worthy gate. Productive duds that prevent wasted loops are wins, not failures.
- **The meta-run (F) MUST be recency-INVERTED, or it just exploits the ledger.** A naive F (allocate over recent outcomes) stays in the orbit of whatever we last did (App Blocks / infra) and never surfaces net-new territory — Zach flagged this directly. Design F with two arms: **explore** (grounded in the NORTH-STAR + the *unexplored* system/product surface, NOT the ledger — maps high-value areas with ZERO prior runs, proposes net-new) + **exploit** (ledger-driven, deepen what we started); tag each candidate explore/exploit and **floor the explore share**. Antidote to recency = go UP to the goal (the value-elicitation interview), not mine recent activity — the same move that broke us out of the infra-loop trap originally. A "coverage map" (high-value surface that exists vs. the tiny slice we've run on) is the explore arm's grounding. **The explore arm DISCOVERS the surface by INTERVIEWING Zach (open, recency-inverted questions), NOT by mining artifacts** (operator decision 2026-06-08) — because artifacts only record what's *been done* (recency-biased by definition); the highest-value *unexplored* surface lives in his head. Interviewing is recency-immune (grounded in judgment/goals) and is the automation of the move that originally broke the infra-loop trap. Corollary to the operating model: **the interview run is the one run type that runs in the MAIN context, not a subagent** — you can't delegate "ask the human."

## The reconciler model (CANON — explored + pressure-tested 2026-06-09)
Map of the loops initiative onto the **Kubernetes control-plane** (declared *desired* state → observe *actual* → diff → apply known-action → repeat; level-triggered, idempotent, self-healing, declarative). It HOLDS for the back half, BREAKS productively for the front half, and the break line is the north-star (judgment vs execution).

**Two regimes (the central partition):**
- **Spec-authoring (front half) — NOT reconcilable, by construction.** Deciding *what* desired state should be; the spec is the unknown, finding it IS the work. K8s reconciles toward specs, it never invents them. = the explore-F **interview**, the genuinely-novel question, the product *intervention*. This is irreducibly Zach's judgment (the bottleneck). Don't try to make it a reconciler — *graduate* its output into a spec.
- **Reconciling (back half) — IS a loop.** Driving observed → a *declared* desired state: predicate + cheap observe + idempotent known-actions + residual-to-agent + write-back. = durability guard, node-saturation, funnel-*visibility*, "every bet ships with its verifier."
- **Flow between regimes IS the productization pipeline:** interview authors a spec → graduate to a deterministic reconciler → residual escalates back to agent → agent's resolutions write back to the spec (shrinking the residual). The control plane is the steady state; the interview is *how a new controller is born*. You never replace the interview — you do it *once per new spec*, then reconcile.

**The gate, split (the single most useful artifact).** The old gate ("bounded + cheap auto verifier") is really:
> **Reconcilable iff (a) desired state is a cheap machine-checkable predicate over observable state, AND (b) the actuator reliably moves observed → desired.**
> Fail (a) → **manual tool** (perf-deep-dive: "good analysis" has no predicate, nothing to diff). Fail (b) → **experiment, not reconciler** (funnel *intervention*: action→effect link unproven; users fight back; can only A/B + measure). This distinguishes two failure modes the single gate lumped together. (a) explains *why* some verifiers are cheap — the desired state is a predicate, not a vibe.

**What the lens unifies / retro-fits:**
- **"Deterministic rail escalates to agent on anomaly" = a level-triggered reconciler.** Rail = observe→diff→apply-known-action (cheap, on cadence). Agent owns ONLY the residual (the non-declarable part: novel drift, "why," "what should desired be"). Agent's resolution **writes back as a new known-action** (= decision-labeling/compound) → residual shrinks → agent budget freed. The reconciler *learns*; the agent is how it learns the non-declarative parts.
- **`surface → VERIFY → route` = observe-before-apply (level-triggered).** Run-3 false positive (routed a B-suspicion before C-verifying) = applying on a stale/assumed state. The C-run (verify) IS the observe step the B-run skipped. Routing must be **idempotent** (don't route what's already enforced — A6 ledger already did it).
- **Graduated autonomy = a controller earning the right to act unattended.** Two distinct admission gates we'd conflated: **spec-admission** (Zach declares/edits desired — rare, high-judgment) vs **action-admission** (a reconciler wants to mutate prod — frequent, low-judgment-each). Decision-labeling acceptance-rate ≥90% retires the **action** gate, never the **spec** gate.
- **Durability guard already fits cleanly.** Its exceptions list (nats=gap; redis=cache; meilisearch=deliberate; sweeps=too-new) **IS a declared desired-state spec**; the operator's one-bit corrections are *edits to the spec*. It's the readiest chain to run the pipeline on.

**From Zach's perspective (the UX — how a reconciler fleet feels day-to-day):** you stop *running* loops and instead *declare* specs + *adjudicate exceptions*. (1) **Spec-authoring session** (rare, ~15min, main context): you dictate a desired-state predicate + exceptions — not code (e.g. the durability spec). Once per controller. (2) **Steady state: you do nothing** — silence = healthy; level-triggered, so new services/missed runs self-catch; no report you're obligated to read (workslop avoided by construction). (3) **Checkpoint → phone**: only when a reconciler wants to mutate prod or hits known drift — one-bit Approve/Deny/"record-exception"; every tap captured by decision-labeling. (4) **Residual escalation**: novel drift → agent returns a *proposal* (drift + why + proposed known-action), not a report to read; your correction writes back. (5) **Autonomy dial**: after ≥90% acceptance on an action class, system offers to stop asking (retires the *action* gate, never the *spec* gate). (6) **Re-authoring**: periodic "is this spec still right?" mini-interview (the spec-drift fix). Experiments (funnel-fixing) are the exception — UX is *propose + pre-wired-verifier + "ship it, I'll tell you if it moved"*, not converge.

**DECISION (2026-06-09, operator): fleet default = GATE EVERY PROD MUTATION.** Read-only reconcilers run silent; ANY action that changes prod requires a phone checkpoint until that action class earns autonomy via acceptance-rate ≥90%. Blast-radius does NOT auto-grant (rejected the "auto-act on low-blast" option); trust-history does. This is already what the clawgate harness implements (checkpoints + decision-labeling + graduated autonomy) — no new mechanism needed to honor it.

**Where it BREAKS (name the limits):**
- **Unreliable actuator** → product/revenue loops are **experiments, not reconcilers** (gate-b). The funnel-*visibility* sub-loop reconciles; the funnel-*fixing* sub-loop is an A/B. Same chain, two different objects.
- **Can't reconcile what you can't observe** — the funnel's channel blind spots = "the controller has no informer for this resource"; "instrument-to-see" = add the status field before you can write a controller on it.
- **Specs drift on their own / adversarially** — pods don't change what they should be; markets do. Reconcilers assume a fixed target during convergence; growth moves it → needs a periodic "is the spec still right?" which routes back to the interview. The control plane never questions its own specs; our system must.
- **Level-triggered upgrade for one-shot runs:** a one-shot run is edge-triggered (`kubectl apply` once, walk away). Make guards re-derive from current observed state every cadence (robust to missed runs, restarts, new services appearing) rather than firing on an event.

## Harness inventory (what's available — reuse, don't rebuild)
- **clawgate** (workbench k3s, `0.6.4` live) — the substrate: dispatches kubeclaw agents that clone a repo; **runbooks** (parameterized dispatch templates); **checkpoints** (human-on-the-loop approval → phone); **privilege profiles** (live RBAC + env + kubeconfig injection); **decision-labeling** (capture + acceptance metric); Prometheus metrics + `/ui/decisions`. Operate via the `/clawgate` skill. Reaches homelab/workbench natively; civit prod via a granted kubeconfig.
- **Existing artifacts:** `perf-deep-dive` runbook + `homelab-observability-read` profile (perf-deep-dive **demoted to a manual tool**, not a loop). 
- **CIVIT PROD CLUSTER ACCESS from the dev host** (the blocker that stalled autonomous infra verification): the DEFAULT kubeconfig/context `admin@civitai-talos` has a STALE CA → `x509: signed by unknown authority`. The WORKING one is **`export KUBECONFIG=/home/zach/workspace/civit/datapacket-talos/prod-kubeconfig`** (context `admin@civit-datapacket-talos`) — read-only verification (rollouts, Flux kustomizations, pod env/logs) works with it. Use this for any datapacket-talos verification; subagents must be told the path (they can't guess it). [[laptop-host]] unrelated.
- **Civit fleet** (separate, in datapacket-talos): capacity-sweep, reliability-sweep P1–P4, dependency-pr-auditor, classifier-tuner, pr-reviewer, alert-diagnose-svc, standup-triage, etc. — check before building anything civit-side (the `next-lever` skill + `AGENTIC_LEVERAGE.md` are the catalog).

## Loop ledger
| Loop | Work-unit | Automatic verifier | State |
|------|-----------|--------------------|-------|
| Decision-labeling | one approve/deny | acceptance-rate metric | **capture infra shipped (0.6.4)** — NOT a closed loop itself; write-back pending |
| **Durability-gap guard** | one stateful service | **backup exists OR a captured accepted-reason** (NOT naive backup-y/n) | **🔄 CLOSING** — audit done (2026-06-07). ⚠️ KEY LESSON: a *blind* check = instrumented-but-unread by construction — 3 of 4 audit "🔴 gaps" dissolved on operator context (orchestration-redis=cache; meilisearch=deliberate tradeoff [index too big, dumps degrade svc]; "dead" sweeps=just-shipped/too-new — the age-not-death trap). The guard MUST capture the operator's one-bit corrections (cache/deliberate/too-new → recorded rule) so it converges to only real gaps; that capture IS this loop merged with the question/compound loop + the 0.6.4 labeling substrate. Standard (confirmed): every stateful system has persistence + an offsite backup, *unless* a recorded reason. **PROOF POINT (2026-06-07): mechanical audit flagged 4 gaps → operator context reduced to 1 real gap (75% false-positive). A blind guard = noise/unread; the value is the context layer.** REAL gap set: **`nats` JetStream (100Gi, no backup) — the only one.** Recorded exceptions (guard must NOT re-flag): orchestration-redis/advertising-redis/feeds-redis/image-cacher-redis/orchestration-next-redis = cache; meilisearch = deliberate tradeoff (index too big, dumps degrade svc); sysredis = replicated; mongo-stale = today's backup-system redo (transient); rightsize/RED/index sweeps = just-shipped (too-new, NOT dead — the age≠death trap). Node-freeze = closing gap (warning→unread digest). **Prod execution (fix nats / build the context-capturing guard) deferred — operator has green-lit touching `datapacket-talos` but "not yet."** When built, seed the exceptions above so the guard runs clean from run 1. |
| Node-saturation guard | one node | memory headroom < threshold (PromQL) | NEXT — born from the node-freeze; flag before freeze (reboot stays human-gated) |
| **CronJob-staleness coverage** | one fleet CronJob | overdue past `kube_cronjob_next_schedule_time` (PromQL, schedule-aware) | **✅ SHIPPED + VERIFIED 2026-06-16.** Audit: 10/20 fleet cronjob levers already had a dedicated `*Stale` rule + all deployments covered generically, but 10 (kyverno-coverage-drafter, dependency-pr-auditor, standup-triage, classifier-tuner×4, pr-review-svc, pr-review-acceptance, pr-fix-svc) had NO staleness alert. Closed with a schedule-aware Grafana **warning** alert (`time() - kube_cronjob_next_schedule_time > 2h`, suspend-guarded; one grace covers weekly `kyverno` + `*/30` pollers alike) → `Warning-Discord`. PR #158 (talos-infra) merged; verified live via Grafana API (rule health:ok, 10 instances, 0 firing). NOT fire-tested (no job currently stale). Origin: a request to *schedule* `fleet-status.sh` — redirected to closing the real gap at the alerting layer (kept fleet-status as the on-demand aggregate it should be). |
| **Question-generation loop** | one well-posed question (of data / of ourselves) + its bounded answer | cheap user up/down "worth asking?" (via decision-labeling) → compound; slow ground-truth = did acting change a decision/metric | **VALIDATED — 2 runs, both high-value.** Run via SUBAGENT (keeps coordinating context clean). **Signature pattern (seen twice): agent surfaces N candidates → operator's one-bit context converges → the real few remain.** Run 1 = durability (A/infra, 4→1: nats). Run 2 = App Blocks (B/product, PR#2319+session: 9 questions → operator resolved 5 + revealed the revenue model + caught 1 real design gap = viewer scope-consent on owner-installed apps). Recurring meta-finding both times: **the bet/system ships without its outcome signal** — fix = ship the verifier WITH the thing. **First end-to-end CLOSE: the App Blocks B-run's finding (viewer scope-consent gap) was ROUTED to its actor** — posted as a structured comment on civitai PR#2319 (issuecomment-4644718999) for the feature-owner agent to pick up. Routing-to-an-actor IS the close (the cure for instrumented-but-unread): ask → converge → route. A surfaced finding that isn't routed is just another unread check. **⚠️ PROCESS FIX (learned the hard way, run 3): the order is surface → VERIFY → route, NOT surface → route.** The App Blocks C-run (run 3, outcome-verify, reused PR#2319 corpus) traced the code and found the §1 viewer-consent "gap" we'd already posted to the PR was a **false positive** — the A6 consent ledger already enforces viewer-level money-path consent. We routed a B-run *suspicion* as an [ACTION] before C-verifying it → a feature agent might build redundant consent. **Run types check each other** (B surfaces candidates → C verifies against ground truth: catches false positives AND finds the false-negatives B can't see — here the real open hole was **A8 privileged-build RCE**, in the gpu-fleet Tekton Task not the PR, gates money-path GA). This is the "audit false-positives AND false-negatives — do both" rule, validated. Don't route unverified findings. Run-type taxonomy now CANON (A & B validated): A data/infra · B self/product · C outcome-verify · D diff-triage · E hypothesis · F aim-selection (meta — now has 2 outcomes to begin allocating over; still early). |
| **Toil-elimination rail (meta-loop)** | one forced-input category over threshold | **that category's frequency DROPS in a later same-window run** (measured by the rail itself) | **🔄 RAIL BUILT 2026-06-17, escalation pending.** Zach's ask: "check my work daily + apply the Algorithm to what I spend time on." Built it as a reconciler, NOT a daily-analysis agent (that = workslop, fails the gate). Deterministic rail = `devrc/scripts/session-analysis/toil-scan.py` (no LLM): measures forced-input categories (slash-cmds + typed-genesis intents) over a trailing window, diffs vs prior **same-window** snapshot (state in `~/.claude/toil-state/`), flags rising/new ≥threshold, suppresses `addressed.json` (the one-bit "already automated OR is real work" memory). Verified working on real data; surfaced `/civitai-web-update-env` (7/7d) as the next command-level candidate. **Verifier = built-in: a shipped elimination shows up as that category's count dropping next window** (e.g. the rail will auto-measure whether the ADS loop actually cuts `/investigate-alert`). **HONEST LIMITS:** (1) recency-biased → only finds recurring TOIL to eliminate, NOT net-new leverage (that needs the explore-interview); (2) typed-intent buckets too COARSE to close directly (check/verify=192 is 192 different checks) — actionable signal is command-level + needs finer ritual clustering; (3) needs laptop transcripts synced for full coverage (currently main-host roots). **FIRST ESCALATION RUN done 2026-06-17 (manual demo — engine works end-to-end):** rail flagged `audit/review PR` (87/7d) → agent verified real toil (74 datapacket-talos / 21 civitai / 1 devrc, not a session artifact) → applied Algorithm → verdict BUILD-NEW(thin): auto-run `/audit-pr` from the existing `pre-push` hook on feature-branch pushes (59/74 audits are pre-PR working-tree diffs the async `pr-reviewer` bot CAN'T see → retarget rejected with evidence; that's why it's build-not-retarget). Verifier = toil-scan count drops next window; blast ~0 (audit subagent read-only); gated DRY→shadow→flip like ADS. **Output is MODEST/QoL not revenue, and bursty (74 from ~1 session)** — agent honestly flagged it + a 10-min "is pre-push the right trigger" check before building. KEY meta-takeaway: the big toil win was ADS; remaining toil-mining is modest or too-coarse-to-close → **the next LARGE leverage is net-new (the explore-interview), not more mining** (recency-bias limit, re-confirmed). **NEXT to close the meta-loop: schedule (fixed window) + auto-escalation on flag.** (Operator 2026-06-23: do NOT self-run toil-scan; not satisfied with stopping; the goal is HIS productivity, not project work — App Blocks etc. have a dedicated agent. He stopped answering clarifying questions = "figure out the next step yourself, my input is the failure.")

**auto-audit-on-push — BUILT 2026-06-23** (the toil-run's proposed close, shipped autonomously). Autonomous version of Zach's #1 forced input (PR-audit ritual, 51–87×/wk). `devrc/githooks/`: pre-push dispatcher → backgrounded `audit-on-push.sh` → headless `claude -p "/audit-pr current"` → routes only 🔴/🟡 to clawgate. Shadow/flag-OFF default (`~/.claude/audit-on-push.env`); `install.sh` sets global `core.hooksPath` (NOT run — opt-in, global git config still untouched); composes with repo-local hooks (chains first, propagates exit). **KEY COVERAGE GAP: does NOT fire in `datapacket-talos` (~85% of his audits)** — that repo pins its OWN repo-local `core.hooksPath` (gitops-gate), overriding the global one; covering it needs composing the audit into datapacket-talos's `.githooks/pre-push` (project-repo change, not done — collision risk). Caveats: clawgate notice = `permission` approve/deny card (leaves a card to clear); shadow still spawns the bg audit (token cost) → don't enable globally blind; not verified live e2e. Rollout: install (shadow) → read `~/.claude/audit-on-push.log` ~1wk → flip `on`. Reinforces the week's lesson: ship AUTONOMOUS (fires on push), not opt-in (the `/audit-pr` command he never reached for). Open decisions: cadence (daily-over-7d vs weekly) + escalation autonomy (auto-dispatch agent vs notify-then-human-trigger — lean human-trigger first per workslop/cost risk, graduate via acceptance). |
| **Funnel-visibility (growth)** | one funnel stage's count/rate | mechanical (query result; #1 drop-off is computed) | **✅ RAN + CLOSED (first revenue-proximate, PURE-EXPLORE run — surfaced by explore-F interviewing Zach, recency-immune).** Funnel made visible end-to-end off the ClickHouse ledger (30d): 3.8M traffic → 236K signup (6.2%) → 105K activated/download (44%) → ~3.3K paid (Buzz 2,328 + sub 981, ~1.4% of regs) → 17.9% retention. **#1 LEAK = activated→paid: 97.8% drop, ~103K activated-but-unpaid new users/mo — THE answer to "what moves revenue" (a conversion, not volume, problem: signups −12% while Buzz buyers +8%).** Blind spots (instrument-to-see): acquisition CHANNEL (no referrer/UTM in CH; GA-only + PII-blocked landingPage) and generation-activation (MVs broken/stale; event only 12d old). **ROUTED:** live snapshot → `datapacket-talos/claudedocs/funnel-visibility-snapshot-2026-06-08.md` (PRIVATE repo — civitai repo is PUBLIC, never put business metrics there). Prior funnel docs exist there (`deep-dive-monetization-funnel`, `generation-funnel-source-tags`, `research-network-effects-growth`) — build on, don't redo. **Segmentation DONE + routed** (appended to the same private doc, pushed). Two data-grounded levers for the activated→paid leak: **(1) push first-generation in onboarding** (download-only convert 0.53% vs generate 6.4% vs post 21–37% = 12–70×; Buzz-as-currency makes generate→pay forward, not just correlation) and **(2) drive a day-2 return** (8+ active days 4.5% vs 1-day 0.34% = 13×; 44% come once). Rule-outs: download volume/speed don't separate (grab-and-go convert worse). Bonus: `landingPage` now readable + has partial UTM → acquisition-channel blind spot partially unblockable. **NEXT = a PRODUCT EXPERIMENT (Zach/team), not a run:** ship the onboarding-generation push, verifier = activated→paid rate moves. The loop surfaced the lever + handed over the verifier; the experiment is product work. **CHAIN COMPLETE:** flying-blind → funnel visible → #1 leak → quantified levers, all in `datapacket-talos/claudedocs/funnel-visibility-snapshot-2026-06-08.md`. |
| **Acquisition-channel attribution** | one channel's volume→activation→paid | mechanical (ClickHouse query result) | **✅ RAN 2026-06-17 (Type-A, read-only, via subagent) — PRODUCTIVE NEGATIVE; NOT routed (operator rating pending).** Aimed at the funnel's parked channel blind-spot on the premise that readable `landingPage`+UTM unblocks external attribution. **Premise was partly false:** landingPage = on-site entry PATH not referrer; UTM on only 4.7% of regs (~95% chatgpt.com); plausible.* referrer table dead since 2025-10-23. So external channel stays blind for ~95% of traffic — **don't build a channel-opt loop on this data.** Reconstructed *entry-intent buckets* + the AI-referral channel instead (30d, n=246K, baseline paid 1.16%). KEEPERS: (1) **ChatGPT = silent 4th-largest signup source, 11K/mo (4.5%), free, paid 1.33% — single-point dependency risk** (an OpenAI policy/link change moves 4.5% of signups). (2) **Model-page entry = 27% of signups, best activation (58.9% dl) but WORST paid (0.81%, below baseline)** = the grab-and-go leak made channel-visible; biggest improvable mass. (3) `/pricing` landers 8.87% paid (~7.6× baseline) but self-selecting intent (n=699) — use as a *destination* to route warm traffic, not a channel to scale. Instrumentation fix to actually close the blind spot: revive plausible OR capture UTM+referrer into `userActivities` at signup. Access: `datapacket-talos/scripts/mine/ch-query.sh` (SOPS age key). Self-assessment: productive dud + 1 real correction + 2 decision-grade facts. |
| **Alert-investigation autonomy (inverted-targeting)** | one firing alertname → auto-investigate → remediation PR | **alert clears** (ADS `verify-pending` replay, already live) + PR-merge acceptance | **🔄 CLOSING — PR #162 OPEN (NOT merged), 2026-06-17.** First increment of the **reduce-Zach-input program** (his directive: every input = a system failure; minimize "waiting for Zach to type"). Target = his #1 forced input, reactive alert investigation (`/investigate-alert` 32×). The whole loop was ALREADY LIVE but gated to fire only on NOVEL alerts (deviation-gate suppress + `has_context` auto-invest skip = "expert handles the known ones"). Closed by INVERTING the targeting: new `ADS_AUTO_INVEST_ALLOWLIST` env knob lets recurring-actionable alertnames through both gates (dedup + 5/day budget intact; PR-only, bot can't merge → Zach-merge = the one-tap gated approve per the standing GATE-EVERY-MUTATION decision). Allow-list is **code-verified** (cluster unreachable from host → couldn't traffic-rank): `MongoDiskFillingSoon`/`MongoDiskUsage{Warning,Critical}` (WiredTiger compact) + `CivitaiDpProdHighLatency` (triage-dp recipes) — alertnames whose `cluster_context` describes an ACTION that clears them. DELETE candidates surfaced + NOT automated (Algorithm step-1): `CPUThrottlingHigh`, `NodeMemoryMajorPagesFaults` (self-resolving → raise `for:`/threshold, recommended in PR for Zach). 60 relevant tests pass (10 new); 17 suite failures pre-existing on clean trunk. Rollback = empty the env var. **⚠️ NOT live-verified: code-verified allow-list (not firing-frequency ranked) + suppressed→anchor→Draft-PR flow unit-tested w/ mocked Discord, NOT exercised against a real fire. Merge gate is Zach's; watch first real `MongoDiskFillingSoon`/`CivitaiDpProdHighLatency` after deploy.** This is increment 1 — NEXT: live-rank to expand the allow-list once cluster reachable; `/investigate-network`+`/investigate-orchestration` (13+11) may have NO auto-trigger surface (symptom-not-alert-driven) — separate loop. **AUDITED + HARDENED 2026-06-17** (`/audit-pr` on #162): found 🔴 inert-on-merge (image not bumped — premature "shipped" claim) + 🟡 suppressed-path bypassed cost-cap/load-shed guards + framing overclaim. ALL FIXED: image `v0.9.32` built+pushed to GHCR (digest `64391265…` registry-verified) + deployment.yaml/pyproject bumped; suppressed path now honors cost-cap+global-rate+namespace+load-shed (+2 tests, 62 pass); PR body corrected (flow = auto-investigate → Draft-PR *button* → human click → human merge = TWO human gates). **MERGED 2026-06-17 14:19Z** (squash `eadbf8ce`) → **DEPLOYMENT VERIFIED on-cluster 14:21Z**: Flux reconciled `eadbf8ce` (ADS Kustomization READY, `Applied revision: trunk@sha1:eadbf8ce`); live image = `v0.9.32@sha256:64391265…`; running pod has `ADS_AUTO_INVEST_ALLOWLIST=MongoDiskFillingSoon,MongoDiskUsageWarning,MongoDiskUsageCritical,CivitaiDpProdHighLatency` + healthy (restarts=0, ready, clean startup, single RS). **BEHAVIOR VERIFIED 2026-06-17 via synthetic fire:** one `/alerts` POST of allowlisted `CivitaiDpProdHighLatency` (33 prior fires ≥ MIN_OBS so it's chronic-suppressible; `MongoDiskFillingSoon` had only 1 fire = NOVEL, wouldn't exercise the path — subagent adapted) → logs show `deviation_gate_decision=chronic_suppress` (old=silent) THEN `auto_investigate_started`+`auto_investigate_suppressed_scheduled` (new: allowlist routed to investigate), all re-added guards passed, real Discord thread `1516814293239206019` created. The loop demonstrably does investigate-not-silent. (Did not click Draft-PR; investigation completion line not captured.) **Durably merged — VERIFIED on origin/trunk** (eadbf8ce ancestor; allowlist code in gates.py/main.py + tests + deployment.yaml v0.9.32 + env all present). A subagent's "not in trunk" alarm was a STALE-LOCAL-CHECKOUT false positive (local was 1 commit behind origin) — lesson: `git fetch` + check `origin/trunk`, never grep a local working tree to judge merge state. **TRAFFIC-RANKED (30d):** current allowlist is mis-targeted — Mongo over-included (MongoDiskFillingSoon 1 fire, Usage variants 0), real recurring toil is DP Redis/upstream: ADD `CivitaiDpProdRedisMemoryHigh`(48), `CivitaiDpProdUpstreamTimeout`(41), `CivitaiDpProdRedisEvictions`(39); do NOT add noise `CivitaiDpProdScrapeTargetDown`(221 flappy)/`KubeJobFailed`(66)/`KubeVersionMismatch`/`KubeHpaMaxedOut`. Expansion needs `ADS_AUTO_INVESTIGATE_DAILY_LIMIT` bump (5→~12; ~3 adds × ~1.5/day would hit the shared 5/day cap + starve novel-alert auto-invest). Minor cosmetic: startup log hardcodes stale `"version":"0.9.14"`. **Increment 2 = PR #163 MERGED + ROLLOUT VERIFIED 2026-06-17** (commit `0a392ccff`; forced `flux reconcile` to apply the approved merge; new pod `…z6b9z` healthy restarts=0; live env confirmed = 7-alert allowlist [+`CivitaiDpProdRedisMemoryHigh`/`UpstreamTimeout`/`RedisEvictions`] + `ADS_AUTO_INVESTIGATE_DAILY_LIMIT=12`). Behavior path already proven by the increment-1 synthetic fire (identical code path); the 3 new DP alerts fire ~1.5/day each so real-traffic exercise is imminent. (Minor: in-file comments still say "5/day", left verbatim — stale.) **✅ REAL-TRAFFIC VERIFIED 2026-06-18:** `CivitaiDpProdUpstreamTimeout` (an added alert) fired 2026-06-17 20:06 → chronic_suppress → auto_investigate → investigate_completed (~10min, 0 err) — the allowlist loop demonstrably ran on a genuine alert. ADS overall 24h: **6 auto-investigations, 0 errors, conservative on Draft-PRs (`auto_draft_skipped` frequent → noise concern NOT materializing).** Operator retuned the next-redis alerts + dropped them from allowlist via commit `e0a449558` (now 5 entries: Mongo×3+HighLatency+UpstreamTimeout) — Algorithm step-1 (fix the alert) chosen over step-5 (automate) for Redis; intentional, not regression. **NEW signal the loop surfaced itself:** `AlloyRemoteWriteFailing` auto-investigated 4× in 17h (recurring observability-pipeline failure, NOT allowlisted → will go silent once chronic) — real-problem-or-tune candidate. UNVERIFIED: diagnosis *usefulness* (RCA content is in Discord `#devops-diagnosis` thread `1516896839142478084`, unreadable from host — needs operator eyeball). Cluster access for verification: `KUBECONFIG=…/datapacket-talos/prod-kubeconfig`. |
| civitai product loops (App Blocks etc.) | TBD | TBD | FUTURE — App Blocks + the growth/funnel family above; reuse this playbook as hours free up |
| **ship (two-host converge)** | one config deploy | both hosts `HEAD==origin/main` + `home-manager switch` rc0 (cheap, automatic) | **✅ SHIPPED + DOGFOODED 2026-06-23** (devrc `bcee39f`, PR #6). Deterministic primitive — **no agent in the loop (a rail), but AGENT-invoked** (the whole point). `scripts/ship.sh` converges workbench+laptop: ff-only (skips non-FF hosts, never auto-rebase), stash/pop-protected, verifies HEAD==origin/main + switch rc0. Covers home-manager only (`sudo nixos-rebuild` = human handoff). Dogfood deployed itself to both hosts + caught the laptop silently lagging twice. Born from the agent-rail correction (see amended opt-in lesson), NOT toil-mining. CLAUDE.md points agents at it. |
| ~~perf-deep-dive~~ | per-service analysis | (human judgment) | **DEMOTED** — fails the gate; manual tool only |

**Status:** the question-generation loop is validated (3 runs) and the funnel/growth chain shipped end-to-end (see START HERE). **Durability monitoring SHIPPED** as deterministic sweeps — `dr-sweep` (LIVE 2026-06-09) + `cert-sweep` (LIVE 2026-06-15), each with `*Stale` rules; the context-capturing refinement + `nats` fix + node-saturation guard remain **parked** (operator: "not yet"). **CronJob-staleness coverage gap closed + verified 2026-06-16** (Grafana warning alert, PR #158). perf-deep-dive demoted.

## Operating model (validated)
**Question-loop runs execute in a SUBAGENT** (proven by the durability audit: subagent does the grounded dig → returns a structured short list → the coordinating context synthesizes + the operator rates). This keeps the high-level/strategic context focused and is the manual prototype of a future clawgate "question-loop runbook" (same dispatch shape). A run = point a subagent at ONE corpus → it surfaces a *ranked short list* of the most decision-relevant questions (of-data / of-ourselves) + bounded answers → STOP (not open-ended) → operator thumbs-up/down = the cheap verifier + first compound signal. Run types: A data-interrogation · B self/strategy · C outcome-verification · D change/diff triage · E hypothesis-design · F aim-selection (the meta-run — highest leverage, slowest verifier; now ready, a track record of 3 exists). **Taxonomy is now CANON** — A/B/C validated across the durability, App Blocks, and funnel runs.

## The validation bar (a loop is only "done" when)
Runs on **real un-rigged work** · the **verifier fires automatically** · the output is **acted on** · demonstrated across **≥3 real runs** with a measured signal (FP rate / acceptance / win-held).

### ✅ VERIFY SUCCESS + #223 REVERTED (2026-06-24)
**VERIFY — DeepSeek V4 Pro cards are Opus-tier (read live from the homelab shadow run).** 4 cards inspected: each cites REAL `file:line` + specific commit SHAs (e.g. Civitai-Link `.red`: traced `civitai-link-api.ts:16`→TLS-CN-mismatch→`clFetch{}`→`CivitaiLinkProvider.tsx:88`, found the env-swap plan omits Link, correlated duplicate-of #67341 + 5 adjacent; LoRA-SDXL: found a fix shipped `79e30dbb8`/`24f57b098` but INSUFFICIENT since incidents post-date it). **KEY: on the Merch+Blue-Buzz ticket DeepSeek ON ITS OWN chose NEEDS-DECISION + flagged the payments/legal risk — the exact judgment Haiku LACKED (Haiku confidently mis-drafted these; needed the gate to save it). DeepSeek catches it itself AND the gate backstops.** So DeepSeek V4 Pro > Haiku here = the keeper. CAVEATS: ~6min/ticket (fine for daily delta; 25 first-run ~2.5h); `gh` not authed in-pod → fell back to `git log` (wire GITHUB_TOKEN for PR-search); real DeepSeek $/ticket not yet measured (metered OpenRouter — check usage). **NEXT: retire devrc/Haiku prototype (homelab/DeepSeek canonical); fix gh-auth; daily runs accumulate; graduate shadow→live-notify (cards → clawgate queue) when ready.**
**#223 REVERTED:** civitai-prod task-drafter copy removed via PR #225 (merged 2026-06-24 00:40Z). Confirmed it NEVER deployed (self-gated on uncommitted secrets; `devpod-task-drafter` ns NotFound on dp-1; empty Kustomization inventory). Zero cleanup; the dead build-failing Kustomization auto-prunes on Flux's next reconcile. Drafter now lives solely on homelab.

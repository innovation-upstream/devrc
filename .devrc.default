#!/usr/bin/env sh

export EDITOR='nvim'

export DEV_CLUSTER=k3d-dev-cluster

alias k=kubectl
alias dc=docker-compose
alias b=bazel
alias d=docker

# Use Standard Dev Env Init
source $HOME/cmd/profile.sh


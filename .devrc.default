#!/usr/bin/env sh

# DO NOT put any secrets or user-specific code in here
# to set tokens and customize your shell, please copy this file into `~/.devrc` 
# file and edit that

export K3D_CLUSTER=dev-cluster
export DEV_CLUSTER=k3d-dev-cluster
export SHARED_DEV_CLUSTER=shared-dev-env

# This needs to be set to a gitlab personal access token if the project your working on uses
# private npm modules
export NPM_TOKEN=
